```markdown
# 함수 -> function -> 기능
 * 기능을 할 수 있는 것
 * 함수는 블랙 박스 -> 어떤 방식으로 처리하는지 모르고, 특정 입력 값을 넣으면 결과를 반환
 
함수 생성
def 함수명(매개변수) :
	동작1
	return 반환값

ex) 
def my_sum(lst) :
			total = 0
			for i in lst :
					total += 1
			return total		
* 사용법 : my_sum(numbers)

함수 사용 이유
- 코드의 재사용성
- 가독성
- 유지보수

* 매개변수 : 입력 값을 받는 변수
* 인자 : 넘겨줘야 할 값

** 개발은 퍼즐 맞추기이다.

### return은 함수를 끝낸다

# 삼항연산자
a if (조건) else b
조건이 True면 a, False면 b
```

#### 실습

```markdown
### 문제 1
입력받은 점수에 따라 합격/불합격을 판별하고 메시지를 반환하는 함수 `get_grade_result`를 작성하세요.

**조건:**
1. 점수가 60점 이상이면 "합격"을 반환합니다.
2. 점수가 60점 미만이면 "불합격"을 반환합니다.
3. 만약 점수가 0점보다 작거나 100점보다 크면 "잘못된 점수"를 반환하세요.
```

```python
def get_grade_result(score):
    if score < 0 or score > 100 :
        return "잘못된 점수"
    elif score >= 60 :
        return "합격"
    else :
        return "불합격"

get_grade_result(80)
```

```python

# 테스트
print(get_grade_result(85))  # 결과: "합격"
print(get_grade_result(40))  # 결과: "불합격"
print(get_grade_result(120)) # 결과: "잘못된 점수"
```

```markdown
### 문제 2
입력받은 숫자가 홀수인지 짝수인지 판별하여 문자열을 반환하는 함수 `check_even_odd`를 작성하세요.

**조건:**
1. 숫자가 짝수이면 "짝수"를 반환합니다.
2. 숫자가 홀수이면 "홀수"를 반환합니다.
```

```python
def check_even_odd(n):
    if n % 2 == 0 :
        return "짝수"
    else :
        return "홀수"

```

```python
def check_even_odd(n):
    if n % 2 :
        return "홀수"
    
    return "짝수"
```

```python
def check_even_odd(n):
   return '홀수' if n % 2 else '짝수'
```

```python

# 테스트
print(check_even_odd(10)) # 결과: "짝수"
print(check_even_odd(7))  # 결과: "홀수"
```

```markdown
### 문제 3
두 개의 숫자와 연산자(+, -)를 입력받아 계산 결과를 반환하는 간단한 계산기 함수 `simple_calc`를 작성하세요.

**조건:**
1. `op`가 "+"이면 두 수의 합을 반환합니다.
2. `op`가 "-"이면 두 수의 차를 반환합니다.
3. 그 외의 연산자가 들어오면 "지원하지 않는 연산"이라는 메시지를 반환하세요.
```

```python
num1 = 10
num2 = 20
op = '+'

def simple_calc(num1, num2, op):
    if op == '+' :
        return num1 + num2
    elif op == "-" :
        if num1 < num2 :
            return num2 - num1
        return num1 - num2
    else :
        return "지원하지 않는 연산"

```

```python

# 테스트
print(simple_calc(10, 5, "+")) # 결과: 15
print(simple_calc(10, 5, "-")) # 결과: 5
print(simple_calc(10, 5, "*")) # 결과: "지원하지 않는 연산"
```

```markdown
### 문제 4
이름 리스트를 입력받아 이름이 3자 이상인 사람만 골라 새로운 리스트로 반환하는 함수 `filter_long_names`를 작성하세요.

**조건:**
1. `for`문을 사용하여 리스트의 각 이름을 확인합니다.
2. `len()` 함수를 사용하여 이름의 길이를 확인합니다.
```

```python
name_list = ["김철수", "이영희", "박씨", "최도날드"]

def filter_long_names(name_list):
    result = []
    for name in name_list :
        if len(name) >= 3 :
            result.append(name)
    return result 

```

```python

# 테스트
name_list = ["김철수", "이영희", "박씨", "최도날드"]
print(filter_long_names(name_list)) # 결과: ["김철수", "이영희", "최도날드"]
# print([name for name in name_list if len(name) >= 3])
```

```markdown
### 문제 5
회원가입 서비스에서 유저가 입력한 데이터의 유효성을 검사하는 함수 `validate_user_registration`을 작성하세요.

**조건:**
1. `username`은 4자 이상이어야 합니다.
2. `age`는 19세 이상이어야 합니다.
3. `email`에는 반드시 '@' 기호가 포함되어야 합니다.
4. 위 조건을 모두 만족하면 `True`, 하나라도 실패하면 `False`를 반환합니다.
```

```python
username = 'abcd1234'
age = 20
email = "admin@example.com"
def validate_user_registration(username, age, email):
    result = False
    if len(username) >= 4 :
        result = True
        if age >= 19 :
            result = True
            if '@' in email :
                result = True
    return result
# 이렇게 실제 서비스에 입력받으면 위쪽 부터 통과해야한다
```

```python

# 테스트
print(validate_user_registration("kimpython", 25, "test@example.com")) # 결과: True
print(validate_user_registration("kim", 25, "test@example.com"))      # 결과: False
```

```python
def validate_user_registration(username, age, email):
    if not len(username) >= 4 :
        return False
    if not (age >= 19) :
        return False
    if '@' not in email :
        return False
    return True
```

```python

# 테스트
print(validate_user_registration("kimpython", 25, "test@example.com")) # 결과: True
print(validate_user_registration("kim", 25, "test@example.com"))      # 결과: False
```

```python
def validate_user_registration(username, age, email) :
    return len(username) >= 4 and age >= 19 and '@' in email
```

```python

# 테스트
print(validate_user_registration("kimpython", 25, "test@example.com")) # 결과: True
print(validate_user_registration("kim", 25, "test@example.com"))      # 결과: False
```

```markdown
### 문제 6
장바구니에 담긴 상품들의 최종 결제 금액을 계산하는 함수 `calculate_checkout_price`를 작성하세요.

**조건:**
1. `membership_grade`가 'VIP'이면 총액의 15%, 'GOLD'이면 10%를 할인합니다.
2. 할인 적용 후 최종 금액이 50,000원 미만이면 배송비 3,000원을 추가합니다.
3. 50,000원 이상이면 배송비는 무료(0원)입니다.
```

```python
my_cart = [{
    "price": 25000, 
    "quantity": 1
    }, {"price": 15000, "quantity": 2}]

def calculate_checkout_price(my_cart, membership_grade):
    total_price = 0
    for cart in my_cart :
        total_price += cart['price'] * cart['quantity']
    if membership_grade == 'VIP' :
        total_price = total_price * 0.85
    elif membership_grade == "GOLD" :
        total_price = total_price * 0.9
    if total_price < 50000 :
        total_price += 3000
    return round(total_price)

```

```python
# 유지보수에 용이하게 만들기 기능별로 분리 시킨다음 최종 함수에서 실행 시킬 수 있게!

my_cart = [{
        "price": 25000, 
        "quantity": 1
    }, {"price": 15000, "quantity": 2}]

def calculate_discount_rate(membership_grade):
    # membership_grade를 가지고 discount_rate를 알려주는 함수.
    # 할인률 계산
    discount_rate_mapping = {
        'VIP' : 0.15,
        'GOLD' : 0.1,
    }
    return discount_rate_mapping.get(membership_grade, 0)

def calculate_shipping_fee(total):
    shipping_fee = 3000
    base_price = 50000

    return 0 if total >= base_price else shipping_fee

def calculate_total_price(my_cart) :
    total = 0
    for product in my_cart:
        price = product['price']
        quantity = product['quantity']
        total += price * quantity
    return total

def calculate_checkout_price(my_cart, membership_grade):

    # 물건 총 합계 계산
    total = calculate_total_price(my_cart)

    # 할인 적용
    discount_rate = calculate_discount_rate(membership_grade)
    total = total * (1 - discount_rate)

    # 배송비 적용
    total = total + calculate_shipping_fee(total)

    return int(total)

```

```python

my_cart = [{"price": 25000, "quantity": 1}, {"price": 15000, "quantity": 2}]
print(calculate_checkout_price(my_cart, "GOLD")) # 결과: 52500
```

```python
def calculate_checkout_price(my_cart, membership_grade):

    # 물건 총 합계 계산
    total = calculate_total_price(my_cart)

    # 할인 전 배송비 계산
    shipping_fee = calculate_shipping_fee(total)

    # 할인 적용
    discount_rate = calculate_discount_rate(membership_grade)
    total = total * (1 - discount_rate)

    # 할인 전 금액 기준의 배송비 적용 - 정답보다 3000원 적다.
    total = total + shipping_fee

    return int(total)
```

```markdown
### 문제 8
특정 조건을 만족하는 유저들을 추출하여 알림 대상자 리스트를 반환하는 함수 `get_notification_targets`를 작성하세요.

**조건:**
1. `is_active`가 `True`인 유저 중에서 선별합니다.
2. `last_login`이 7일 이상이거나, `point`가 10,000점 이상인 유저가 대상입니다.
3. 대상자의 'name'만 리스트에 담아 반환하세요.
```

```python
users_data = {
    "u1": {"name": "Alice", "is_active": True, "last_login": 10, "point": 5000},
    "u2": {"name": "Bob", "is_active": False, "last_login": 1, "point": 15000},
    "u3": {"name": "Charlie", "is_active": True, "last_login": 2, "point": 12000}
}

def get_notification_targets(users_data):
    li = []
    for user in users_data.values() :
        if user['is_active'] == True and (user['last_login'] >= 7 or user['point'] >= 10000) :
            li.append(user['name'])
    return li

```

```python

users_data = {
    "u1": {"name": "Alice", "is_active": True, "last_login": 10, "point": 5000},
    "u2": {"name": "Bob", "is_active": False, "last_login": 1, "point": 1500},
    "u3": {"name": "Charlie", "is_active": True, "last_login": 2, "point": 12000}
}
print(get_notification_targets(users_data)) # 결과: ['Alice', 'Charlie']
```

```python
def get_notification_targets(users_data):
    li = []
    for user in users_data.values() :
        if not user['is_active'] :
            continue
        if not (user['last_login'] >= 7 or user['point'] >= 10000) :
            continue
        li.append(user['name'])
    return li
```

#### 수업

```markdown
# 모듈(Module)
 : 특정 기능을 하는 코드를 파이썬 파일 단위로 작성한 것
   모듈을 이용하면 다른 사람이 작성한 기능을 가져와 사용할 수 있다

imoort math

# 올림이란 기능을 만들고 싶어.
def ceiling(num) :
		if num > int(num) :
				return num+1
		return num
이렇게가 아니라

print(math.ceil(2.3)) -> 이런식으로 가져와서 쓸 수 있다

## 기능 골라서 쓰기
from math import floor
floor()
```

#### 실습

```markdown
### 문제 1
여러 명의 학생 성적 데이터가 담긴 복합 리스트입니다. 각 학생의 `grades` 리스트에서 80점 이상의 점수만 추출하여 평균을 계산하고, 그 평균이 90점 이상인 학생의 이름만 `top_students` 리스트에 담으세요.
```

```python
students = [
    {"name": "John", "grades": [85, 92, 78, 100]},
    {"name": "Sarah", "grades": [88, 95, 90, 89]},
    {"name": "Tyler", "grades": [70, 75, 82, 80]},
    {"name": "Emma", "grades": [95, 98, 88, 92]}
]
top_student = []

def get_average(grades) :
    total = 0
    count = 0
    for grade in grades :
        if grade >= 80 :
            total += grade
            count += 1
    if count == 0 :
        return 0
    average = total / count

    return average

def get_average(grades) :
    grades = [grade for grade in grades if grade >= 80]

    average = sum(grades) / len(grades)
    if len(grades) == 0 :
        return 0
    return average

for student in students  :
    name = student['name']
    grades = student['grades']
    average = get_average(grades)

    if average >= 90 :
        top_student.append(name)
print(top_student)
```

```markdown
### 문제 2
소셜 미디어 API 응답 데이터 형태입니다. 각 유저(`users`)가 작성한 게시글(`posts`)들 중, 좋아요(`likes`) 수가 50개 이상인 게시글의 제목(`title`)만 모두 모아 하나의 리스트로 만드세요.
```

```python
api_response = {
    "status": "success",
    "data": {
        "users": [
            {
                "username": "user1",
                "posts": [
                    {"title": "오늘의 식단", "likes": 30},
                    {"title": "여행 사진", "likes": 150}
                ]
            },
            {
                "username": "user2",
                "posts": [
                    {"title": "코딩 공부법", "likes": 88},
                    {"title": "운동 일지", "likes": 10}
                ]
            },
            {
                "username": "user3",
                "posts": [
                    {"title": "ai 공부법", "likes": 808},
                    {"title": "일기", "likes": 100}
                ]
            },
        ]
    }
}

popular_titles = []

# 코드를 작성하세요
# 소셜 미디어 API 응답 데이터 형태입니다. 
# 각 유저(`users`)가 작성한 게시글(`posts`)들 중, 좋아요(`likes`) 수가 50개 이상인 게시글의 제목(`title`)만 모두 모아 하나의 리스트로 만드세요.
for user in api_response['data']['users'] :
    # print(user)
    for post in user['posts'] :
        likes = post['likes']
        title = post['title']
        if likes >= 50 :
            popular_titles.append(title)

print(f"인기 게시글 제목: {popular_titles}")
```

```python
from pprint import pprint

api_response = {
    "status": "success",
    "data": {
        "users": [
            {
                "username": "user1",
                "posts": [
                    {"title": "오늘의 식단", "likes": 30},
                    {"title": "여행 사진", "likes": 150}
                ]
            },
            {
                "username": "user2",
                "posts": [
                    {"title": "코딩 공부법", "likes": 88},
                    {"title": "운동 일지", "likes": 10}
                ]
            },
            {
                "username": "user3",
                "posts": [
                    {"title": "ai 공부법", "likes": 808},
                    {"title": "일기", "likes": 100}
                ]
            },
        ]
    }
}

popular_titles = []
data = api_response['data']
users = data['users']
# pprint(users)
for user in users :
    # pprint(user)
    posts = user['posts']
    # print(posts)

    for post in posts :
        # print(post)
        title = post['title']
        likes = post['likes']

        if likes >= 50 :
            popular_titles.append(title)
print(f"인기 게시글 제목: {popular_titles}")

# 추가문제 : user별 좋아요 50개 이상인 게시글의 제목 모아보기

```

```markdown
### 문제 3
상품 리스트를 카테고리별로 분류하여 딕셔너리를 만드세요.
```

```python
products = [
    {"category": "전자제품", "name": "키보드"},
    {"category": "의류", "name": "티셔츠"},
    {"category": "전자제품", "name": "마우스"},
    {"category": "전자제품", "name": "노트북"},
    {"category": "식품", "name": "사과"},
    {"category": "식품", "name": "배"},
    {"category": "의류", "name": "청바지"}
]

grouped_data = {}

# 코드를 작성하세요
for product in products :
    category = product['category']
    name = product['name']
    print(category, name)
    if category not in grouped_data :
        grouped_data[category] = []

    grouped_data[category].append(name)    

print(f"그룹화 결과: {grouped_data}")
```

```python
from collections import defaultdict
grouped_data = defaultdict(list)
for product in products :
    # print(product)
    category = product['category']
    name = product['name']
    # print(category, name)

    grouped_data[category].append(name)
    

print(f"그룹화 결과: {grouped_data}")
```

음음 접근하는 방법이 조금 달라서 참신했다.

저런식으로 주는 딕셔너리가 있으려나 잘 모르겠다 ㅋㅋㅋ

그래도 이해는 됬으니 다행이다. 오늘도 머리 아프게 공부 해버렸네… 하하