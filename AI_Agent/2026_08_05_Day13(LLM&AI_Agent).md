```markdown
### 복습

- AI 
* 생성형 : 텍스트(LLM), 이미지, 영상, 음성(TTS[텍스트를 음성], STT[음성을 텍스트])
* 확률형

- LLM : 엔트로픽, 챗지피티, 재미나이
* Input -> AI -> OutPut

1. 환각
- 그럴듯한 거짓
2. 지식 컷오프
- 학습 데이터가 끝나는 시점 이후 일은 모름, 옛날 데이터
 * 도구 연결
3. 컨텍스트 윈도우
- 나와 대화를 나누는 맥락 Context-Memory 단기기억력 (1M 4~6권 정도의 책 분량)
* 세션이 가득찰 경우 : 컴팩트(압축), 새 세션 시작(작업내용 기억, md + commit)
** 기획서(계획) -> md, 작업 -> TODO, md파일 저장, commit

하나의 세션만 사용시
- 토큰이 많이 든다(Input 토큰이 점점 늘어가기 때문에)
- 올드 세션 -> 예전꺼를 참고할 수 밖에 없다

새 세션 시작 시 읽어오는 파일이 있다
-> ex) 
- 로컬
claude.md (프로젝트 설명)
 * 참조형으로 넣으면 좋다

- 글로벌 

4. 비결정성
- 같은 질문에도 매번 조금씩 다른 답이 나옴, 확률형 토큰을 뽑기 때문

AI Agent
* 구조가 있다. 규칙 적용 등과 같은 것

프롬프트 4요소 
 : 역할(누구인지) - 맥락(어떤 상황, 배경) - 작업(할 일, 디테일) - 형식(결과물 모양)

Zero-shot : 예시 없이 지시만으로 시키는 방식 (* 이거 나온다고 함)
Few-shot : 예시를 넣어서 시키는 방식

3일차 체인 어쩌구 간단하게 읽어보기

#### n8n
- 각각의 노드로 이루어져있다.
- 트리거 노드 : 워크플로우 자동화 도구에서 자동화 프로세스가 시작되는 출발점
- if 노드 : 데이터의 분기를 만들어주는 노드 (참/거짓)
- merge : 병합
- split Out : 여러개의 데이터가 하나로 들어오는데 그걸 나눠주는 노드
- item이 넘어오는 만큼 반복된다
- Credential : 내가 어떤 자격으로 다수의 서비스를 쓸지 인증하는 것 
```

```markdown
ox문제
1. LLM은 질문의 뜻일 이해하고 답한다. 
2. 같은 내용이라도 한국어가 영어보다 토큰을 더 많이 쓴다
3. LLM API 요금은 입력 토큰과 출력 토큰을 각각 계산한다
4. 모델은 모르는 것을 물으면 모른다고 답한다
5. 모델이 실시간 시세를 모르는 이유는 성능이 부족해서이다
6. 지피티가 최신 정보를 잘 답하는 것은 모델이 더 똑똑하기 때문이다.
7. 프롬프트 잘 쓰는 것은 마법의 주문을 외우는 것에 가깝다
8. 프롬프트에 너는 ~다 라고 역할 을 주면 수준이 달라진다
9. 자동화에 쓸 프롬프트에서 가장 중요한 것은 출력 형식이다 -> o
10. 매번 내용은 달라도 형식은 일정하게 만드는 것이 자동화에서 중요하다 -> o
```

```markdown
RSS는 사이트가 새 글 목록을 기계가 읽기 좋게 공개하는 표준
* 크롤링보다 안정적이고 합법적인 수집 경로
```

그 외 미니프로젝트 및 시험 공부 준비