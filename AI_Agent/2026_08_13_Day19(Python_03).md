```markdown
## 실습 후 강사님 버전 

# 1.
a = list(range(6))

a = []
num = 0
while num < 6 :
    a.append(num)
    n += 1

while True :
    a.append(num)
    if num == 5 :
        break
    num += 1

b = []
for num in range(4) :
    b.append(num + 4)

b = [num + 4 for num in range(4)]

b = list(range(8))
b = b[4:]

d = []
for _ in range(4) :
    d.append(1)
    d.append(2)

# 3.
num = 3
while num < 40 :
    print(num, end = " ")
    num += 3
    

n = 40
for i in range(n//3) : # 구구단 코드 재활용 -> 40 이하의 3의 배수 개수는 3으로 나눈 몫
		print(3 * (i + 1), end= " ")

# 4.
print(100//7)

print(len([i for i in range(1, 101) if i % 7 == 0]))

# 7. 
a = 4
b = 6

num = a
total = 0
while num <= b :
    total += num
    num += 1

print(total)

a와 b의 수가 바뀔 경우 0만 출력되게 된다

a = 5
b = 2

if a > b :
    a, b = b, a

num = a
total = 0

while num <= b :
    total += num
    num += 1

print(total)

if 문으로 변수의 값을 바꾼다

# 8.
for index in range(len(colors)) :
    color =colors[index]
    print(index, color)

# 11.
result = 0 
while True :
    input_int = int(input())
    print(input_int)

    if input_int == -1 :
        break

    if result < input_int : 
        result = input_int
print(result)

# 13.
n = 17
is_prime = True
for num in range(2, n) :
    if n % num == 0 :
        is_prime = False
        break
print(is_prime)
```

#### 참고 : 변수를 만들 때는 함수 같은 것들은 사용하지 않는다.

```markdown
문자열
 : 컨테이너
* 인덱싱
 * 문자열은 수정이 불가능

* f-string
- 문자 사이에 변수를 넣을 수 있다.
.format은 옜날 방식이다

strip
 : 특정 문자(기본값은 공백)을 제거한다

# Set
 : 중복 허용하지 않으며 순서 없이 저장하는 컨테이너
   {} 중괄호 사용

* [] list, {} set, () tuple

집합 연산 교집합, 합집합, 차집합 연산 제공

in 연산 -> list 보다 효율적이다
- True/False 반환

set() 존재 - {}, 여러 data 순서x 중복 제거

# tuple
 : 변경이 불가능한 list

# 딕셔너리(Dict)
 : key : value 쌍을 순서 없이 저장하는 컨테이너 
  * 여러개의 data를 구분 지으면서 
* students = {"kyle": 10, "alex": 20}
* 키는 중복 X

students = {}

students["kyle"] = 10  # 삽입
print(students)

조회 - .get으로도 가능

keys() - 모든 키 목록
values() - 모든 밸류 목록
items() - 모든 키, 밸류 목록
```

#### 실습

```markdown
---
### 문제 1
`scores` 딕셔너리에 있는 모든 학생의 이름을 한 줄에 하나씩 출력하세요.
```

```python
scores = {
    "Alice": 85,
    "Bob": 92,
    "Charlie": 78,
    "David": 95,
    "Eve": 88
}

for i in scores.keys() :
    print(i)

for i in scores :
    print(i)
```

```markdown
### 문제 2
`scores` 딕셔너리의 모든 점수를 더하여 총합을 출력하세요.
```

```python
scores = {
    "Alice": 85,
    "Bob": 92,
    "Charlie": 78,
    "David": 95,
    "Eve": 88
}

total = 0
for i in scores.values() :
    total += i
print(total)

```

```markdown
### 문제 3
모든 학생의 이름과 점수를 `Alice : 85` 와 같은 형식으로 출력하세요.
```

```python
# 코드를 여기에 작성하세요
scores = {
    "Alice": 85,
    "Bob": 92,
    "Charlie": 78,
    "David": 95,
    "Eve": 88
}

for name, score in scores.items() :
    print(f"{name} : {score}")
```

```markdown
### 문제 4
기존 `scores` 딕셔너리를 사용하여 점수가 90점 이상인 '우수 학생'의 명단을 리스트 형식으로 만들어 출력하세요.
```

```python
# 코드를 여기에 작성하세요
li = []
for name, score in scores.items() :
    if score >= 90 :
        li.append(name)
print(li)
    
```

```markdown
### 문제 5
기존 `scores` 딕셔너리를 사용하여 점수가 80점 이상이면 'Pass', 미만이면 'Fail'을 값으로 가지는 새로운 딕셔너리를 만드세요.
```

```python
# 코드를 여기에 작성하세요
di = {}

for name, score in scores.items() :
    if score >= 80 :
        di[name] = "Pass"
    else :
        di[name] = "Fail"
print(di)
```

```markdown
### 문제 1
API로 전달받은 게시글 데이터입니다. `status`가 'published'인 게시글들만 필터링하여, 해당 게시글들의 `view_count` 총합을 구하세요.
```

```python
posts = [
    {'title': '첫 번째 글', 'status': 'published', 'view_count': 120},
    {'title': '두 번째 글', 'status': 'draft', 'view_count': 0},
    {'title': '세 번째 글', 'status': 'published', 'view_count': 350},
    {'title': '네 번째 글', 'status': 'deleted', 'view_count': 10}
]
total = 0
for i in posts :
    if i['status'] == 'published' :
        total += i['view_count']
print(total)

```

```markdown
### 문제 2
유저 권한 시스템 로직입니다. 아래 규칙에 따라 접근 가능 여부(`True`/`False`)를 판단하여 결과를 적절한 형태로 만드세요.
1. `role`이 'admin'이면 점수와 상관없이 `True`
2. `role`이 'user'이고 `score`가 80점 이상이면 `True`
3. 그 외(guest 등)는 모두 `False`
```

```python
users = [
    {'name': 'Kim', 'info': {'role': 'admin', 'score': 50}},
    {'name': 'Lee', 'info': {'role': 'user', 'score': 85}},
    {'name': 'Park', 'info': {'role': 'user', 'score': 70}},
    {'name': 'Choi', 'info': {'role': 'guest', 'score': 100}}
]
access_results = []

for user in users :
    info = user['info']
    if info['role'] == 'admin' or (info['role'] == 'user' and info['score'] >= 80) :
        access_results.append(user['name'])

print(f"접근 권한 결과: {access_results}")
```

```markdown
### 문제 3
쇼핑몰의 카테고리별 상품 데이터입니다. 전체 상품 중 가격(`price`)이 50,000원 이상인 상품의 이름(`name`)만 모아서 하나의 리스트로 만드세요.
```

```python
store_data = {
    "electronics": [
        {"name": "키보드", "price": 45000},
        {"name": "모니터", "price": 200000}
    ],
    "fashion": [
        {"name": "티셔츠", "price": 20000},
        {"name": "청바지", "price": 60000},
        {"name": "재킷", "price": 120000}
    ],
    "furniture": [
        {"name": "의자", "price": 200000},
        {"name": "책상", "price": 600000},
        {"name": "책장", "price": 1200000}
    ]
}
li = []
for data in store_data :
    category_list = store_data[data]
    for item in category_list :
        price = item['price']
        if price >= 50000 :
            li.append(item['name'])
print(li)
```

```python
product_li = []
for products in store_data.values() :
    for product in products :
        # print(product)
        name = product['name']
        price = product['price']

        if price >= 50000 :
            product_li.append(name)
print(product_li)
```

```markdown
### 문제 4
주문 내역 데이터입니다. 각 주문 별로 'items' 리스트가 들어있습니다. 전체 주문을 통틀어 '품절(out_of_stock)' 상태가 포함된 주문의 `order_id` 리스트를 구하세요. 또한 품절 상태인 items의 name에 대한 list를 구하세요.
```

```python
orders = [
    {
        "order_id": "ORD-001",
        "items": [
            {"name": "사과", "status": "in_stock"},
            {"name": "배", "status": "in_stock"}
        ]
    },
    {
        "order_id": "ORD-002",
        "items": [
            {"name": "포도", "status": "out_of_stock"},
            {"name": "딸기", "status": "in_stock"}
        ]
    },
    {
        "order_id": "ORD-003",
        "items": [
            {"name": "수박", "status": "out_of_stock"}
        ]
    },
    {
        "order_id": "ORD-004",
        "items": [
            {"name": "참외", "status": "out_of_stock"},
            {"name": "멜론", "status": "out_of_stock"}
        ]
    },
    {
        "order_id": "ORD-005",
        "items": [
            {"name": "망고", "status": "out_of_stock"},
            {"name": "토마토", "status": "in_stock"},
            {"name": "호두", "status": "out_of_stock"}
        ]
    },
]

problematic_orders = []
list_name = []
# 코드를 작성하세요

for order in orders :
    # print(order)
    items = order['items']
    # print(items)
    for item in items :
        # print(item)
        if item['status'] == 'out_of_stock' :
            list_name.append(item['name'])
    for item in items :
        if item['status'] == 'out_of_stock' :
            problematic_orders.append(order['order_id'])
            break
print(f"품절 포함 주문 ID: {problematic_orders}")
print(list_name)
```

```python
out_li = []
for order in orders :
    # print(order)
    order_id = order['order_id']
    items = order['items']
    # print(items)

    for item in items :
        # print(item)
        status = item['status']
        if status == 'out_of_stock' :
            # print('out_of_stock')
            out_li.append(order_id)
            break # 하나의 주문에서 중복으로 넣는 것을 방지
print(out_li)
```

```python
out_of_stock_items = []

for order in orders :
    items = order['items']
    for item in items :
        name = item['name']
        status = item['status']
        # print(name, status)
        if status == 'out_of_stock' :
            out_of_stock_items.append(name)
print(out_of_stock_items)
```

```python
problematic_orders = []
list_name = []
# 코드를 작성하세요

for order in orders :
    # print(order)
    items = order['items']
    # print(items)
    for item in items :
        # print(item)
        name = item['name']
        status = item['status']
        if status == 'out_of_stock' :
            list_name.append(name)

            if order['order_id'] not in problematic_orders :
                problematic_orders.append(order['order_id'])
print(f"품절 포함 주문 ID: {problematic_orders}")
print(list_name)
```

```python

# 전체 주문을 통틀어 '품절(out_of_stock)' 상태가 포함된 주문의 `order_id` 리스트를 구하세요. 

out_of_stock_orders = []
out_of_stock_items = []

for order in orders:

    order_id = order['order_id']
    items = order['items']

    has_out_of_stock = False 
    # 상태를 items를 돌면서 그때그때 바꾸는게 아니라, 하나의 order에 대해서 변수로 활용합니다.

    for item in items:
        name = item['name']
        status = item['status']
        if status == 'out_of_stock':
            has_out_of_stock = True
            out_of_stock_items.append(name)

    if has_out_of_stock:
        out_of_stock_orders.append(order_id)

print(f"품절 포함 주문 ID: {out_of_stock_orders}")
print(f"품절 아이템: {out_of_stock_items}")

```

와… 오늘 너무 어렵다.. 머리가 터질거 같으니깐 다시 풀어보자 복습 복습!!!!!!

조금더 열심히 해보자 화이팅!