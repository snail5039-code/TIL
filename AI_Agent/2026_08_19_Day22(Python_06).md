#### 강사님 과제 풀이

### step.1

```markdown

# 다음 2차원 배열을 만들어보세요.

#  1  2  3  4  5
# 16 17 18 19  6
# 15 24 25 20  7
# 14 23 22 21  8
# 13 12 11 10  9

n = 5

# 2차원 배열에 숫자를 채워넣기 위해서
# 1. 리스트를 만들어서 하나하나 집어넣는다
    # [] -> [[]] -> [[1, 2, 3, 4]] -> [[1, 2, 3, 4], [5, 6, 7, 8]]
# 2. 미리 특정 사이즈의 2차원 배열을 만들어서 값만 넣는다.

from pprint import pprint
snail = [[0]*n for _ in range(n)]

# snail = []

# for _ in range(n): # n번 반복할꺼야.
#     snail.append([0] * n)

pprint(snail)

# 왼쪽 위에서 시작해서 
# 숫자를 하나씩 키워가면서 
# 한칸씩 이동을한다.

# 기초 정보
i = 0
j = 0
num = 1

# 방향성을 특정지어야 함.
#     우  하  좌  상
di = [0, 1, 0, -1]
dj = [1, 0, -1, 0]

# 우리의 진행 방향.
d = 0

# 1넣기
snail[i][j] = num

print()
pprint(snail)

# 2 넣기
num += 1

i = i + di[d]
j = j + dj[d]

snail[i][j] = num
print()
pprint(snail)

# 3 넣기
num += 1

i = i + di[d]
j = j + dj[d]

snail[i][j] = num
print()
pprint(snail)
```

### step.2

```python
# 반복의 과정을 정하기 위해서 주석만 바꿈.

# 기초 정보
snail = [[0]*n for _ in range(n)]
i = 0
j = 0
num = 1

# 방향성을 특정지어야 함.
#     우  하  좌  상
di = [0, 1, 0, -1]
dj = [1, 0, -1, 0]

# 우리의 진행 방향.
d = 0

# 1넣기 -> 숫자 바꾸고 이동하기
snail[i][j] = num

print()
pprint(snail)

num += 1

i = i + di[d]
j = j + dj[d]

# 2 넣기 -> 숫자 바꾸고 이동하기
snail[i][j] = num
print()
pprint(snail)

num += 1

i = i + di[d]
j = j + dj[d]

# 3 넣기 -> 숫자 바꾸고 이동하기
snail[i][j] = num
print()
pprint(snail)

num += 1

i = i + di[d]
j = j + dj[d]

```

### step.3

```python
# 범위를 벗어난 index -> 실행이 안되는게 맞는 코드입니다.

# 기초 정보
snail = [[0]*n for _ in range(n)]

i = 0
j = 0

# 방향성을 특정지어야 함.
#     우  하  좌  상
di = [0, 1, 0, -1]
dj = [1, 0, -1, 0]

# 우리의 진행 방향.
d = 0

# 1넣기 -> 숫자 바꾸고 이동하기
for num in range(1, 10):
    snail[i][j] = num

    print()
    pprint(snail)

    # (0,n)에서 범위를 벗어남.
    i = i + di[d]
    j = j + dj[d]

```

### step.4

```python
# 범위를 벗어난 index -> 실행이 안되는게 맞는 코드입니다.

# 기초 정보
snail = [[0]*n for _ in range(n)]

i = 0
j = 0

# 방향성을 특정지어야 함.
#     우  하  좌  상
di = [0, 1, 0, -1]
dj = [1, 0, -1, 0]

# 우리의 진행 방향.
d = 0

# 1넣기 -> 숫자 바꾸고 이동하기
for num in range(1, n**2+1):
    snail[i][j] = num

    print()
    pprint(snail)

    # (0,n)에서 범위를 벗어남.
    # grid를 벗어난 경우 -> 방향을 바꿔줘야 함.
    # grid를 벗어난 경우 인덱스 관리가 어려워서
    # 내가 직접 이동하는게 아니라, (ni, nj)라는 이동의 후보군을 미리 보내놓습니다.

    ni = i + di[d]
    nj = j + dj[d]

    # ni, nj가 grid를 벗어나지 않아 -> 그냥 그대로 이동해
    # ni, nj가 grid를 벗어나 -> 방향 바꾸고 다시 이동해 -> 특별한 경우
    if not (0 <= ni < n and 0 <= nj < n):
        # 방향 이동
        d += 1
        # 새로 이동한 좌표
        ni = i + di[d]
        nj = j + dj[d]

    # 이동
    i = ni
    j = nj
```

### step.5

```python
# 방향을 나타내는 d를 변경

from pprint import pprint

n = 5
snail = [[0]*n for _ in range(n)]

i = 0
j = 0

# 방향성을 특정지어야 함.
#     우  하  좌  상
di = [0, 1, 0, -1]
dj = [1, 0, -1, 0]

# 우리의 진행 방향.
d = 0

# 1넣기 -> 숫자 바꾸고 이동하기
for num in range(1, n**2+1):
    snail[i][j] = num

    print()
    pprint(snail)

    ni = i + di[d]
    nj = j + dj[d]

    # 현재는 grid를 벗어났을 때에만 방향을 바꿈.
    # -> 이미 값이 있을때도 방향을 바꿈.
    # 즉, snail[ni][n]에 뭔가 있을 때, snail[ni][nj] != 0
    if not ((0 <= ni < n) and (0 <= nj < n)) or snail[ni][nj]:
        # d가 4가 되어버리면 index error 발생.
        # d가 4가 되면 0으로 바꿔줌.
        # d += 1
        # if d == 4:
        #     d = 0

        # 4로 나눈 나머지
        d = (d + 1) % 4

        ni = i + di[d]
        nj = j + dj[d]

    # 이동 
    i = ni
    j = nj
```

### step.6

```python

from pprint import pprint

n = 5
snail = [[0]*n for _ in range(n)]

# 시작점
i = 0
j = 0

# 방향 후보
di = [0, 1, 0, -1]
dj = [1, 0, -1, 0]

# 방향
d = 0

for num in range(1, n**2+1):
    snail[i][j] = num

    ni = i + di[d]
    nj = j + dj[d]

    # 그리드를 벗어났을 때 또는 이미 값이 있을 때
    if not ((0 <= ni < n) and (0 <= nj < n)) or snail[ni][nj]:

        d = (d + 1) % 4

        ni = i + di[d]
        nj = j + dj[d]

    i = ni
    j = nj
```

한단계씩 처음에는 그림을 그리고 

그거를 말로 풀고

간단한 코드로 작성

반복 파악

문제점 파악

해결방안 도출 이런 흐름으로 푸는 연습을 해야겠다

#### 이론

```markdown
array list
# list : 순서가 있게 여러개의 데이터 저장
1. index 찾기 O(1) -> 시간복잡도
2. value 찾기 'abc' 들어있니? -> if 'abc' in list
 * O(n) 시간복잡도 발생
3. 삽입/ 삭제 : O(1) 거의 시간이 들지 않는다
 * 문제는 앞 쪽 삽입/ 삭제 : 모든 것들이 새로 만들어지는 느낌 O(n)만큼의 시간이 걸림
** index로 접근하는데 좋음

# linked list 
 : 링크 처럼 하나하나 이어져있다.
** 다음 데이터 위치를 알고 있기 때문에 삽입, 삭제에 유용하다(맨 앞과 뒤에서)
   다만 index로 접근은 좋지 않다
- 이미 만들어져있다. degue
- 스택(접시를 쌓는 것) -> 들어오는 것과 나가는 것이 같다
```

#### 프로그래머스 실습

```markdown
문제 설명
괄호가 바르게 짝지어졌다는 것은 '(' 문자로 열렸으면 반드시 짝지어서 ')' 문자로 닫혀야 한다는 뜻입니다. 예를 들어

"()()" 또는 "(())()" 는 올바른 괄호입니다.
")()(" 또는 "(()(" 는 올바르지 않은 괄호입니다.
'(' 또는 ')' 로만 이루어진 문자열 s가 주어졌을 때, 문자열 s가 올바른 괄호이면 true를 return 하고, 올바르지 않은 괄호이면 false를 return 하는 solution 함수를 완성해 주세요.

제한사항
문자열 s의 길이 : 100,000 이하의 자연수
문자열 s는 '(' 또는 ')' 로만 이루어져 있습니다.
```

```python
def solution(s):

    # 올바른 괄호 란 것을 만들고 싶습니다.
    # 열린 괄호 : +1
    # 닫힌 괄호 : -1

    # edge case
    # 1 : )( -> 합은 0이지만, 닫히고 열리기 때문에 안됨.
    # -> 
    value = 0
    for char in s:
        if char == '(':
            value += 1
        elif char == ')':
            value -= 1

        # 1번째에 대한 해결책
        if value < 0:
            return False

    if value == 0:
        return True
    else:
        return False

print(solution("(())()"))      # True
print(solution("(()(")  )      # False     # 열리는게 더 많은 경우
print(solution("(()())))") )   # False     # 닫히는게 더 많은 경우
print(solution(")()(")     )   # False     # 개수는 같지만, 닫히는게 먼저 오는 경우.

# 확장 -> () -> () , {}, []
from collections import deque
def solution(s):

    stack = deque() # list를 써도 무방하긴 합니다.
    open_blanket = {'(', '{', '['}

    blanket_mapping = {
        ']' : '[',
        '}' : '{',
        ')' : '(',
    }
    # 열린게 들어와 -> stack에 넣어줘.
    # 닫힌게 들어와 -> stack의 가장 뒤의 녀석을 확인해서
        # 쌍이 맞으면 성공
        # 쌍이 맞지 않으면 실패

    for char in s:
        # list보다는 set이 `in`에 대해 속도가 빠름.
        # list -> O(n) / set -> O(1)
        if char in open_blanket:
            stack.append(char)

        # elif char == ']':
        #     # stack의 맨 뒤는 [ 여야 해.
        # elif char == '}':
        #     # stack의 맨 뒤는 { 여야 해.
        # elif char == ')':
        #     # stack의 맨 뒤는 ( 여야 해.
        # elif char 가 닫히는 것
        #     stack의 맨 뒤는 blanket_mapping[char] 여야 해.
        elif char in blanket_mapping.keys(): # char가 닫히는 것 이라면.

            # stack이 비어있는 상태에서 닫히는게 들어오면 False
            # 즉, 닫히는게 더 많은 경우
            if not stack:
                return False
            
            # 쌍이 맞지 않는 경우
            if stack[-1] != blanket_mapping[char]:
                return False
            
            stack.pop()
    

    # 열리는게 남아있는 경우도 있습니다.
    if stack:
        return False
    
    return True

print()
print(solution("[{}][()]"))         # True
print(solution("([{}][()]")  )      # False     # 열리는게 더 많은 경우
print(solution("[{}][()])") )       # False     # 닫히는게 더 많은 경우
print(solution("[{)][()]"))         # False     # 쌍이 맞지 않는 경우.
```

```markdown
문제 설명
트럭 여러 대가 강을 가로지르는 일차선 다리를 정해진 순으로 건너려 합니다. 모든 트럭이 다리를 건너려면 최소 몇 초가 걸리는지 알아내야 합니다. 다리에는 트럭이 최대 bridge_length대 올라갈 수 있으며, 다리는 weight 이하까지의 무게를 견딜 수 있습니다. 단, 다리에 완전히 오르지 않은 트럭의 무게는 무시합니다.

예를 들어, 트럭 2대가 올라갈 수 있고 무게를 10kg까지 견디는 다리가 있습니다. 무게가 [7, 4, 5, 6]kg인 트럭이 순서대로 최단 시간 안에 다리를 건너려면 다음과 같이 건너야 합니다.

경과 시간	다리를 지난 트럭	다리를 건너는 트럭	대기 트럭
0	[]	[]	[7,4,5,6]
1~2	[]	[7]	[4,5,6]
3	[7]	[4]	[5,6]
4	[7]	[4,5]	[6]
5	[7,4]	[5]	[6]
6~7	[7,4,5]	[6]	[]
8	[7,4,5,6]	[]	[]
따라서, 모든 트럭이 다리를 지나려면 최소 8초가 걸립니다.

solution 함수의 매개변수로 다리에 올라갈 수 있는 트럭 수 bridge_length, 다리가 견딜 수 있는 무게 weight, 트럭 별 무게 truck_weights가 주어집니다. 이때 모든 트럭이 다리를 건너려면 최소 몇 초가 걸리는지 return 하도록 solution 함수를 완성하세요.

제한 조건
bridge_length는 1 이상 10,000 이하입니다.
weight는 1 이상 10,000 이하입니다.
truck_weights의 길이는 1 이상 10,000 이하입니다.
모든 트럭의 무게는 1 이상 weight 이하입니다.
입출력 예
bridge_length	weight	truck_weights	return
2	10	[7,4,5,6]	8
100	100	[10]	101
100	100	[10,10,10,10,10,10,10,10,10,10]	110
```

```python
from collections import deque
def solution(bridge_length, weight, truck_weights):
    answer = 0

    # truck_weights에 들어있는 트럭들을
    # bridge_length라고 되어있는 queue에 하나씩 통과를 시켜서
    # 시간을 재고 싶다.

    # 단, bridge는 무게 제한이 있다.

    # 1. bridge에 truck_weights의 첫번째 트럭 넣기.
    # 2. bridge위의 첫번째 트럭을 왼쪽으로 옮기고, 
    #    truck_weights의 다음 트럭 준비시키기.
        # 만약 들어가면 넣기
        # 안들어가면 기다리기
        # 언젠가는 들어감.
    #    ....

    bridge = deque([0]*bridge_length) # qeueue.
    bridge_weight = 0 # 다리에 있는 버스들의 무게
    ready_truck_index = 0

    time = 0
    # bridge_weight > 0 => 다리에 아직 버스가 있다.
    # ready_truck_index < len(truck_weights) -> 준비된 버스가 있다.

    while bridge_weight > 0 or ready_truck_index < len(truck_weights): 
        out_bus = bridge.popleft() # 원래 있던 버스 빼기.
        bridge_weight -= out_bus    # 나간 버스만큼 무게가 빠진다.

        # 버스 준비시키기.
        # 다리 위의 무게 + 준비한 버스의 무게 <= 버틸 수 있는 무게
        # 이동을 시킬 수 있음.
        # ready_truck_index < len(truck_weights) -> 준비된 트럭이 존재할때만 아래 코드를 진행해.
   

        if ready_truck_index < len(truck_weights) and bridge_weight + truck_weights[ready_truck_index] <= weight:

            truck = truck_weights[ready_truck_index]

            bridge.append(truck)
            bridge_weight += truck
            ready_truck_index += 1
        else:
            bridge.append(0) # 빈공간 채워주기
            # bridge_weight += 0
            # ready_truck_index 변함이 없습니다.

        time += 1

    return time
```

와 너무 어렵다 이거는 내가 다시 풀어봐야겠다…. 오늘 해결하고 잔다…!!

#### 이론

```markdown
# API

# HTTP 프로토콜
 * 웹에서 데이터를 주고받기 위한 통신 규약
 * 클라이언트와 서버 사이에서 메세지를 어떻게 주고받을지 정해놓은 약속

클라이언트 : 서비스를 요청하는 주체, 웹 브라우저/모바일 웹
서버 : 요청에 대해 응답을 제공하는 주체

비연결성 
 : 요청을 보내고 서버가 응답을 마치면 즉시 연결을 끊는 방식

무상태성
 : 각 요청이 독립적, 서버가 클라이언트의 이전 상태를 기억하지 않는다
 
# 요청(Requset) 메시지
메서드(어떻게), URL 주소(누구에서), Data(무엇을)

#  응답 메시지
HTTP, status, Data

# HTTP 메소드
* GET 조회
* POST 만들어줘(생성)
 : 서버한테 명령 무언가를 저장해줘
* PUT 수정
* PATCH 수정(일부분)
* DELETE 삭제

# HTTP 상태 코드(응답)
* 100
* 200 : 성공
* 300 : 다른데로 넘김(리다이렉션)
* 400 : 클라이언트 오류
* 500 : 서버 오류

HTTP/HTTPS
-> 보안 계층을 추가한 프로토콜
* 암호화, 데이터 무결성, 인증

# RESTful API
## 인터페이스
 * 서로 다른 두 개의 시스템이나 장치가 정보를 주고 받을 때 발생하는 접점
 -> CLI, GUI(그림)
 
API 
 : 소프트웨어 어플리케이션이 서로 데이터를 주고받으며 기능을 수행할 수 있도록 정의한 규격
 -> 다른데서도 프로그램을 사용할 수 있게 해주는 것

RESTful API
 : 웹 상에서 요청 응답을 받을 때 효율적으로 관리하기 위한 설계 지침?

특징
클라이언트와 서버 분리 / 무상태성 / 캐시 처리 가능 / 계층화 시스템 / 
인터페이스의 일관성(URL, method, data)

URI : 자원 고유 식별 체계
 URL : 자원이 어디에 위치하는지 나타내는 물리적인 주소
 URN : 위치와 상관없이 자원에 부여된 고유한 이름
 
특징
스키마(scheme) : 통신 규약 HTTPS
Domain name : 아이피 주소, 웹 서버 주소
Port : 문, 우편번호
Path : 서버 내부 실제 자원이 위치한 상세 경로 (변수 같은 느낌)
Parameters : 서버에 전달하는 추가적인 정보, 물음표 뒤에 &로 구분
Anchor : 문서 내부의 특정 위치를 가리키는 책갈피 같은 역할

응답과 데이터 처리 간 JSON 형태

API 엔드포인트 => URL

* 대부분의 리소스는 복수다
* 계층 관계를 나타내는 경로 구조
 ex) /users/1/orders
* HTTP 메서드와 CRUD 기능의 매핑
 : 주소는 명사로 고정하고, 해당 리소스에 대해 무엇을 할지는 메서드로 구분
** 경로 변수 : 자원 식별 / 쿼리 매개변수 : 정렬, 필터, 검색

URL은 하이픈 사용 user-profiles
```

**## API 활용**

```markdown
terminal에서

`pip install requests`

명령어를 통해 requests 모듈을 다운받습니다
```

```python
import requests
from pprint import pprint

url = "https://open.er-api.com/v6/latest/USD"
rseponse = requests.get(url)
# print(type(rseponse))
# print(dir(rseponse))
# print(rseponse.url)
# print(rseponse.status_code)
# print(rseponse.headers)
data = rseponse.json()
pprint(rseponse.status_code)
print(data['rates']['KRW'])
pprint(data)

# 한국 환율을 알고싶어.

```

```markdown
## requests에서 사용하는 HTTP 메서드

`requests`는 HTTP 요청을 보내기 위한 파이썬 라이브러리이다. API의 목적에 따라 다음 메서드를 사용한다.

- `requests.get(url)`: 서버에서 데이터나 목록을 조회하는 메서드이다.
- `requests.post(url, json=data)`: 서버에 새로운 데이터를 생성하는 메서드이다.
- `requests.patch(url, json=data)`: 기존 데이터 중 필요한 일부만 수정하는 메서드이다.
- `requests.put(url, json=data)`: 기존 데이터를 새로운 내용으로 전체 교체하는 메서드이다.
- `requests.delete(url)`: 서버의 데이터를 삭제하는 메서드이다.

### 요청 옵션

- `params`: URL 뒤에 쿼리 매개변수를 추가할 때 사용한다. 예를 들어 `requests.get(url, params={"userId": 2})`는 `?userId=2`를 URL에 추가한다.
- `headers`: 인증 정보, 응답 형식 등 요청의 메타데이터를 전달할 때 사용한다. 예를 들어 `headers={"Authorization": "Bearer <token>"}`과 같이 인증 토큰을 전달한다.

각 메서드는 `Response` 객체를 반환한다. 응답 본문은 `response.json()`으로 확인하고, 요청 성공 여부는 `response.status_code` 또는 `response.raise_for_status()`로 확인한다.

또는 `response.text`로도 본문 확인이 가능함. 단, 이는 str이다.
```

```markdown
### 자원 조회 (GET)
특정 데이터를 가져오거나 전체 목록을 불러올 때 사용한다.
```

```python
# 단일 리소스 조회
url = "https://jsonplaceholder.typicode.com/posts/10"
response = requests.get(url)
# print(\"단일 조회 결과:\", response.json())

data = response.json()
pprint(data)
print(data['body'])
```

```python
# 전체 목록 조회
list_url = "https://jsonplaceholder.typicode.com/posts"
list_response = requests.get(list_url)
print("전체 게시글 개수:", len(list_response.json()))
```

```markdown
### 자원 생성 (POST)
새로운 데이터를 서버에 등록한다. 데이터는 JSON 형식으로 전달된다.
```

```python
url = "https://jsonplaceholder.typicode.com/posts"

new_data = {
    "title": "하하 호호",
    "body": "새로운 내용",
    "userId": 1
}

response = requests.post(url, json=new_data)
print("생성 성공 상태 코드:", response.status_code)
print("응답 데이터:", response.json())
```

```markdown
### 자원 수정 (PATCH)
데이터의 일부를 변경을 요청한다.
```

```python
url = "https://jsonplaceholder.typicode.com/posts/1"

# 일부 수정
patch_data = {"title": "수정된 제목"}
patch_response = requests.patch(url, json=patch_data)
print("수정 결과:", patch_response.json())
```

```markdown
### 전체 자원 수정 (PUT)
기존 자원의 모든 필드를 새 데이터로 교체한다.
```

```python
url = "https://jsonplaceholder.typicode.com/posts/1"

# 전체 수정
put_data = {
    "title": "PUT으로 수정된 제목",
    "body": "PUT 요청으로 전체 내용을 교체합니다.",
    "userId": 1
}

put_response = requests.put(url, json=put_data)
print("전체 수정 상태 코드:", put_response.status_code)
print("수정 결과:", put_response.json())
```

```markdown
### 자원 삭제 (DELETE)
데이터 삭제를 요청한다.
```

```python
url = "https://jsonplaceholder.typicode.com/posts/1"

# 삭제
delete_response = requests.delete(url)
print("삭제 성공 상태 코드:", delete_response.status_code)
```

```markdown
### 필터링 (params)
보조적인 조건을 추가하여 정보를 가져온다.
```

```python
# 쿼리 매개변수를 활용한 필터링
# query_url = "https://jsonplaceholder.typicode.com/posts?userId=2"
# filter_response = requests.get(query_url)

query_url = "https://jsonplaceholder.typicode.com/posts"
params = {"userId": 2}
filter_response = requests.get(query_url, params=params)

print("사용자 1의 게시글 수:", len(filter_response.json()))
pprint(filter_response.json())
```

```markdown
### 중첩 리소스 조회 (GET)
특정 자원에 종속된 정보를 가져온다.
```

```python
# 중첩 리소스 조회
nested_url = "https://jsonplaceholder.typicode.com/posts/1/comments"
nested_response = requests.get(nested_url)
print("1번 게시글의 첫 번째 댓글:", nested_response.json()[0])
pprint(nested_response.json())
```

```markdown
### 예외 처리
요청 중 발생할 수 있는 네트워크 오류와 HTTP 오류를 처리한다.

- `Timeout`: 설정한 시간 안에 서버 응답이 오지 않을 때 발생한다.
- `HTTPError`: 서버에는 연결됐지만 404(자원 없음), 401(인증 필요), 500(서버 오류)처럼 4xx나 5xx 상태 코드를 응답할 때 발생한다.
- `raise_for_status()`: 응답 상태 코드가 200~399면 아무 일도 하지 않는다. 400대나 500대면 `HTTPError`를 발생시켜 `except`문에서 처리할 수 있게 한다. 호출하지 않으면 500이어도 예외는 발생하지 않고, `response.status_code`에 500이 담긴다.
- `RequestException`: 위 예외들을 포함한 요청 관련 오류의 상위 예외다. 별도로 분기할 필요가 없는 오류를 처리한다.

```

```python
error_url = "https://jsonplaceholder.typicode.com/posts/1"

try:
    response = requests.get(error_url, timeout=5)
    response.raise_for_status()
    print("요청 성공:", response.json())
except requests.exceptions.Timeout:
    print("요청 시간이 초과되었습니다.")
except requests.exceptions.HTTPError as error:
    print("HTTP 오류가 발생했습니다:", error)
except requests.exceptions.RequestException as error:
    print("요청 중 오류가 발생했습니다:", error)
```