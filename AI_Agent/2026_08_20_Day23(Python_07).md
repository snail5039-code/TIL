```markdown
### 예외 처리
요청 중 발생할 수 있는 네트워크 오류와 HTTP 오류를 처리한다.

- `Timeout`: 설정한 시간 안에 서버 응답이 오지 않을 때 발생한다.
- `HTTPError`: 서버에는 연결됐지만 404(자원 없음), 401(인증 필요), 500(서버 오류)처럼 4xx나 5xx 상태 코드를 응답할 때 발생한다.
- `raise_for_status()`: 응답 상태 코드가 200~399면 아무 일도 하지 않는다. 400대나 500대면 `HTTPError`를 발생시켜 `except`문에서 처리할 수 있게 한다. 호출하지 않으면 500이어도 예외는 발생하지 않고, `response.status_code`에 500이 담긴다.
- `RequestException`: 위 예외들을 포함한 요청 관련 오류의 상위 예외다. 별도로 분기할 필요가 없는 오류를 처리한다.

```

```python
# 정상
error_url = "https://jsonplaceholder.typicode.com/posts/1"

try:
    response = requests.get(error_url, timeout=5)
    response.raise_for_status()
    print("요청 성공:", response.json())
except requests.exceptions.Timeout:
    print("요청 시간이 초과되었습니다.")
except requests.exceptions.HTTPError as error:
    print("HTTP 오류가 발생했습니다:", error)
except requests.exceptions.RequestException as error:
    print("요청 중 오류가 발생했습니다:", error)
```

```python
# 404 not found

error_url = "https://jsonplaceholder.typicode.com/posts/1111111111"

try:
    response = requests.get(error_url, timeout=5)

    # 400, 500번대도 무사히 클라이언트에 응답이 도착합니다.
    # 그 상황은 client의 입장에서는 data가 정상적으로 오지 않았기때문에 문제상황이라고 할 수 있습니다.
    response.raise_for_status()
    
    print("status code", response.status_code)
    
    print("요청 성공:", response.json())
except requests.exceptions.Timeout:
    print("요청 시간이 초과되었습니다.")
except requests.exceptions.HTTPError as error:
    print("HTTP 오류가 발생했습니다:", error)
except requests.exceptions.RequestException as error:
    print("요청 중 오류가 발생했습니다:", error)
```

```python
# time out

error_url = "https://jsonplaceholder.typicode.com/posts/1"

try:
    response = requests.get(error_url, timeout=0.1)
    response = requests.get(error_url, timeout=5)

    # 400, 500번대도 무사히 클라이언트에 응답이 도착합니다.
    # 그 상황은 client의 입장에서는 data가 정상적으로 오지 않았기때문에 문제상황이라고 할 수 있습니다.
    response.raise_for_status()
    
    print("status code", response.status_code)
    print("요청 성공:", response.json())
except requests.exceptions.Timeout:
    print("요청 시간이 초과되었습니다.")
except requests.exceptions.HTTPError as error:
    print("HTTP 오류가 발생했습니다:", error)
except requests.exceptions.RequestException as error:
    print("요청 중 오류가 발생했습니다:", error)
```

#### API와 연동해서 쓸 때에는 예외처리를 하는 것이 좋다!

```markdown
# 파이썬 예외처리

오류(error)는 프로그램에서 발생하는 문제를 넓게 부르는 말이다. 예외(exception)는 그중 프로그램 실행 중 발생하며 파이썬에서 감지하고 처리할 수 있는 오류 상황을 말한다. 

예외처리는 이러한 상황이 발생했을 때 프로그램이 비정상적으로 종료되지 않도록 하고, 예외를 적절히 처리하거나 복구하기 위해 실행 흐름을 제어하는 방법이다.
```

```markdown
### 기본 예외 처리 구조

try 문 내부에 예외 발생 가능성이 있는 코드를 배치하고, except 문에서 발생한 예외를 처리한다.
```

```python
try:
    # 예외 발생 가능성이 있는 코드
    result = 10 / 0
    # result = 10 / 3
    print(result)
except:
    # 예외 발생 시 실행되는 코드
    print("예외가 발생하였습니다.")

```

```python
a = input()

# # 만약 a가 숫자로 변경이 가능해?
# if a.isnumeric():
#     a = int(a)
#     print(a)
# else:
# # 만약에 a가 숫자로 변경이 불가능해?
#     print('정수로 변경이 불가능합니다.')

try:
    a = int(a)
    print(a)
except:
    print('정수로 변경이 불가능합니다.')

```

```markdown
### 특정 예외 처리

발생할 수 있는 예외의 종류를 명시하여 해당 오류에 대해서만 선택적으로 처리할 수 있다.
```

```python
try:
    # 10/0 # 0으로 나누는 에러는 아래의 IndexError가 아니기 때문에 잡을 수 없음.
    
    numbers = [1, 2, 3]
    # 인덱스 범위 초과 예외 발생
    print(numbers[5])

except IndexError:
    # 특정 예외가 발생했을 때 실행되는 블록
    print("인덱스 범위를 초과하였습니다.")

# numbers = [1, 2, 3]
# # 인덱스 범위 초과 예외 발생
# print(numbers[5])
```

```markdown
### 다중 예외 처리

여러 개의 except 블록을 사용하여 각기 다른 종류의 예외를 개별적으로 처리할 수 있다.
```

```python
try:
    value = int(input())
    result = 10 / value
    print(result)
except ValueError:
    # 데이터 형변환 오류 시 실행
    print("형변환에 실패하였습니다.")
except ZeroDivisionError:
    # 0으로 나누기 오류 시 실행
    print("0으로 나눌 수 없습니다.")
```

```markdown
### 예외 객체 참조

as 키워드를 사용하여 발생한 예외 객체의 인스턴스를 변수에 할당하고 오류 메시지를 확인할 수 있다.
```

```python
try:
    result = 10 / 0
except ZeroDivisionError as e:
    # 예외 객체를 e 변수에 할당
    print(e) # 시스템에서 전달하는 예외 메시지 출력

```

```markdown
### 예외 발생 여부에 따른 추가 제어

else 문은 try 블록에서 예외가 발생하지 않았을 경우에만 실행된다. finally 문은 예외 발생 여부 및 처리 여부와 관계없이 코드 블록의 마지막에 반드시 실행된다.
```

```python
def divide_data(input_value):
    try:
        # 정수 형변환 및 나눗셈 연산
        number = int(input_value)
        calculation = 100 / number
    except ValueError:
        # 유효하지 않은 숫자 형식 입력 시 처리
        print("숫자 형태의 문자가 아닙니다.")
    except ZeroDivisionError:
        # 0 입력으로 인한 연산 불가 시 처리
        print("0으로 나눌 수 없습니다.")
    else:
        # 예외 없이 연산이 성공한 경우 결과 출력
        print(f"연산 결과는 {calculation} 입니다.")
    finally:
        # 성공 및 실패 여부와 무관하게 작업 종료 상태 출력
        print("데이터 처리를 완료했습니다.")

# 정상 실행 예시
divide_data("20")

print()
# 예외 발생 예시
divide_data("0")
```

```markdown
### 예외 강제 발생

raise 키워드를 사용하여 의도적으로 예외를 발생시키고 프로그램의 흐름을 중단하거나 제어할 수 있다.
```

```python
def check_age(age):
    if age < 0:
        # ValueError를 인위적으로 발생시킴
        raise ValueError("나이는 음수일 수 없습니다.")
    return age

try:
    check_age(-5)
except ValueError as e:
    # raise에 의해 발생한 예외를 처리
    print(e)
```

```markdown
### 사용자 정의 예외

기존 예외 클래스를 상속받아 새로운 예외 클래스를 정의하여 사용할 수 있다.
```

```python
class MyCustomError(Exception):
    # 기본 Exception 클래스를 상속받아 사용자 정의 예외 생성
    def __init__(self, message):
        self.message = message

try:
    # 사용자 정의 예외 발생
    raise MyCustomError("사용자 정의 오류가 발생했습니다.")
except MyCustomError as e:
    # 생성한 예외 클래스에 대한 처리
    print(e.message)
```

#### 실습

TMDB API 발급

api 문서 → 상단의 api refernce → 좌측의 movie lists → now playing

https://api.themoviedb.org/3/movie/now_playing 지금은 인증이 안되었음

https://api.themoviedb.org/3/movie/now_playing?api_key=30e6b753XXXXXXXXXXXXXXXXXX

이런식으로 인증을 달아야함

JWT  : 정보를 암호화

- JSON 정보를 가지고 있다. 이거를 가지고 인증한다

```python
# 내가 연습 1 (api_key)

import requests 
from pprint import pprint

url = "https://api.themoviedb.org/3/movie/now_playing"

api_key = "30e6b75xxxxxxxxxxxxxxxxxxxxxxxx"

my_parameter = {
    "api_key" : api_key
}

try : 
    response = requests.get(url, params=my_parameter)
    response.raise_for_status()
    print(response.status_code)
    pprint(response.json())
except Exception as e :
    print("오류발생!")
    print(e)
```

```python
# 내가 연습 2 (token)

import requests 
from pprint import pprint

url = "https://api.themoviedb.org/3/movie/now_playing"

token = "eyJhbGcixxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

my_header = {
    'Authorization' : f"Bearer {token}" 
}

try : 
    response = requests.get(url, headers=my_header)
    response.raise_for_status()
    print(response.status_code)
    pprint(response.json())
except Exception as e :
    print("오류발생!")
    print(e)
```

#### 이론

```markdown
# 환경변수
 : 보안이 필요한 인증 정보 등을 코드 외부에서 관리하기 위해 사용
   운영체제 수준에서 정의되는 동적 변수, 소프트웨어 실행 시 참조되는 설정 값 저장
* 보안성 확보/환경 독립성/유지보수 효율

# 파이썬 환경변수 조작 방법
 : 표준 라이브러리 os 모듈을 사용하여 환경변수에 접근 및 제어

gitignore.io에 들어가면 이그노어를 만들 수 있다.

* git과 같은 버전 관리에서는 .env는 관리되지 않아야한다
```

#### api 활용 실습

```markdown
### 문제 1
현재 상영 중인 영화(Now Playing) 중 평점(vote_average)이 가장 높은 영화를 찾으시오.
```

```python
url = "https://api.themoviedb.org/3/movie/now_playing"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}

try:

    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    # pprint(data)
    results = data.get('results')
    # pprint(result)
    
    max_value = 0
    best_movie = ""
    for movie in results :
        # print(movie)

        vote_average = movie.get('vote_average')
        # print(vote_average)
        # print()
        if max_value < vote_average :
            max_value = vote_average
            best_movie = movie.get('title')
    print(best_movie, max_value)
    
except Exception as e:
    print('오류 발생!')
    print(e)

```

#### 람다 사용

```python
url = "https://api.themoviedb.org/3/movie/now_playing"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}

try:
    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    # pprint(data)
    results = data.get('results')
    # pprint(result)
    
    best_movie = max(results, key=lambda x : x['vote_average'])
    print(best_movie['title'], best_movie['vote_average'])
    
except Exception as e:
    print('오류 발생!')
    print(e)

```

```markdown
### 문제 2
인기있는 영화(Popular) 중 평점(vote_average)이 가장 높은 영화를 찾으시오.
```

```python
url = "https://api.themoviedb.org/3/movie/popular"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}

try:
    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    # pprint(data)
    results = data.get('results')
    # pprint(result)
    
    best_movie = max(results, key=lambda x : x['vote_average'])
    print(best_movie['title'], best_movie['vote_average'])
    
except Exception as e:
    print('오류 발생!')
    print(e)
```

#### 함수 사용

```python
# 1. 영화 데이터를 가져온다.
# 2. 영화 데이터를 바탕으로 평점이 가장 높은 영화를 찾는다.

def get_movies(what) :
    # movie/ 뒤에 now_playing or popular
    url = f"https://api.themoviedb.org/3/movie/{what}"
    token = os.getenv("token")

    my_headers = {
        "Authorization" : f"Bearer {token}",
    }

    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    
    results = data.get('results')
    return results

def get_best_movie(movies) :
    
    best_movie = max(movies, key=lambda x : x['vote_average'])
    return best_movie

try :
    movies = get_movies('now_playing')

    best_movie = get_best_movie(movies)
    print(best_movie['title'], best_movie['vote_average'])

except Exception as e :
    print('오류 발생!')
    print(e)
```

```markdown
### 문제 3
현재 상영 중인 영화(now_playing) 데이터에서 다음 정보만 담긴 리스트를 만드세요.

- title (영화 제목)  
- vote_average (평점)  
- release_date (개봉일)
```

```python
url = "https://api.themoviedb.org/3/movie/now_playing"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}

try:

    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    # pprint(data)
    results = data.get('results')
    # pprint(results)
    
    lst = []

    for movie in results :
        
        title = movie.get('title')
        vote_average = movie.get('vote_average')
        release_date = movie.get('release_date')
        lst.append([title, vote_average, release_date])
    pprint(lst)

    
except Exception as e:
    print('오류 발생!')
    print(e)

```

```python
def get_simple_movie(movie) :
    title = movie['title']
    vote_average = movie['vote_average']
    release_date = movie['release_date']

    simple_movie = {
        "title" : title,
        "vote_average" : vote_average,
        "release_date" : release_date
    }
    return simple_movie
try :
    movies = get_movies('now_playing')
    simple_movies = [get_simple_movie(movie) for movie in movies]

    pprint(simple_movies)

except Exception as e :
    print(e)
```

```markdown
### 문제 4
현재 상영 중인 영화(now_playing) 데이터를 vote_average(평점)순으로 정렬하여 영화 제목이 담긴 리스트를 만드세요.
단, 리스트의 길이는 20 이상이어야 합니다.

```

```python
url = "https://api.themoviedb.org/3/movie/now_playing"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}
try:
    res1 = requests.get(url, headers=my_headers, params={"page": 1})  
    res1.raise_for_status()
    results1 = res1.json().get("results", []) 

    res2 = requests.get(url, headers=my_headers, params={"page": 2})
    res2.raise_for_status()  
    results2 = res2.json().get("results", [])  

    results = results1 + results2 
    
    lst = []

    for movie in results :
        
        title = movie.get('title')
        vote_average = movie.get('vote_average')
        lst.append([title, vote_average])
    lst.sort(key=lambda x : x[1], reverse=True)
    pprint(lst)

    
except Exception as e:
    print('오류 발생!')
    print(e)

```

```python
try:
    # 아래 로직을 함수로 만들어서
    # n페이지까지의 정보를 한번에 가져오는 함수를 만들수도 있을겁니다.
    pages = [1, 2, 3, 4]
    many_movies = []
    for page in pages:

        movies = get_movies(page=page)
        # many_movies += movies
        many_movies.extend(movies)
    # pprint([movie['title'] for movie in many_movies])
    # many_movies를 활용해서 vote_aveate 순으로 정렬 -> 영화 제목만 가져오기

    # sorted 
    # key 사용해서 기준 잡기
    # reverse=True -> 역순으로 정렬

    sorted_movies = sorted(many_movies, key=lambda movie : movie['vote_average'], reverse=True)

    sorted_movies_title = [(movie['title'], movie['vote_average']) for movie in sorted_movies]

    pprint(sorted_movies_title)

except Exception as e:
    print(e)
```

```markdown
### 문제 5
현재 상영 중인 영화 중 평점(vote_average)이 가장 높은 영화의 수익(revenue)를 찾으시오.  
hint) API 문서의 endpoint 항목을 참고하세요.
```

```python
url = "https://api.themoviedb.org/3/movie/now_playing"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}

try:
    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    # pprint(data)
    results = data.get('results')
    # pprint(results)
    
    best_average = max(results, key=lambda x : x['vote_average'])

    best_movie_id = best_average['id']

    best_movie_url = f"https://api.themoviedb.org/3/movie/{best_movie_id}"

    best_response = requests.get(best_movie_url, headers=my_headers)

    best_movie_data = best_response.json()
    pprint(best_movie_data['revenue'])
    
except Exception as e:
    print('오류 발생!')
    print(e)
```

```python
## 강사님꺼 들어갈 자리
def get_movie_by_id(movie_id):
    url = f"https://api.themoviedb.org/3/movie/{movie_id}"

    token = os.getenv("token")

    my_params = {
        # language
        # region
    }

    my_headers = {
        "Authorization" : f"Bearer {token}",
    }

    response = requests.get(url, headers=my_headers, params=my_params)

    # 함수 내부에서는 raise만 하고, except 처리는 밖에서 진행합니다.
    # 항상 그래야하는건 아닙니다.
    response.raise_for_status()

    data = response.json()
    
    return data
```

```python
try:

    # 1. 현재 상영중인 영화 가져오기
    movies = get_movies()
    # 2. 평점이 가장 높은 영화 가져오기
    movie = get_best_movie(movies)

    # 2.a revenue가 0이라 첫번째 영화로 대체하기
    movie = movies[0]
    # print(movie)

    movie_id = movie.get('id')
    # 3. 그 영화의 detail 정보 가져오기 -> revenue 찾기.
    movie_detail = get_movie_by_id(movie_id)

    # pprint(movie_detail)
    revenue = movie_detail.get('revenue')

    print(revenue)

except Exception as e:
    print(e)
```

```markdown
### 문제 6
현재 상영 중인 영화 중 평점(vote_average)이 가장 높은 영화의 포스터 이미지를 조회하시오.  
hint) API 문서의 `Guides`를 참고하세요

```

```python
url = "https://api.themoviedb.org/3/movie/now_playing"

token = os.getenv("token")

my_headers = {
    "Authorization" : f"Bearer {token}",
}

try:
    response = requests.get(url, headers=my_headers)

    response.raise_for_status()

    data = response.json()
    # pprint(data)
    results = data.get('results')
    # pprint(results)
    
    best_average = max(results, key=lambda x : x['vote_average'])

    best_movie_id = best_average['id']

    best_movie_url = f"https://api.themoviedb.org/3/movie/{best_movie_id}"

    best_response = requests.get(best_movie_url, headers=my_headers)

    best_movie_data = best_response.json()
    pprint(best_movie_data['poster_path'])
    
except Exception as e:
    print('오류 발생!')
    print(e)
```

```python
try:

    movies = get_movies()
    # print([movie['poster_path'] for movie in movies])
    movie = get_best_movie(movies)
    pprint(movie)

    pprint(movie['poster_path'])

except Exception as e:
    print(e)
```

오늘은 생각보다 어렵지는 않았는데 함수로 기능을 빼는게 조금 어색해서 나는 빼지를 못하고 강사님꺼를 보면서 저런식으로 하는 거구나 했다!