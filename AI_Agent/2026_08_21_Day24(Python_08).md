#### 강사님 풀이

- 전날에 풀었던 것과 연계해야 한다.

```markdown
### 문제 7
현재 상영 중인 영화 중 평점(vote_average)이 가장 높은 영화의 배우(Credit / cast) 정보를 조회하시오.  
hint) API 문서의 endpoint 항목을 참고하세요

```

```python
# 모든 요청들을 아우르는 함수를 하나 만들껍니다.
def fetch_tmdb_api(path, my_params = {}):
    # movie/ 뒤에 now_playing or popular
    url = f"https://api.themoviedb.org/3/{path}"

    token = os.getenv("token")
    my_headers = {
        "Authorization" : f"Bearer {token}",
    }

    response = requests.get(url, headers=my_headers, params=my_params)

    response.raise_for_status()

    data = response.json()
    return data
```

```python
try:
    movies = get_movies()
    # movies = fetch_tmdb_api(f"movie/now_playing", my_params={"page":1})

    best_movie = get_best_movie(movies)
    # print(best_movie)

    movie_id = best_movie['id']

    # movie_detail = get_movie_by_id(movie_id)
    # pprint(movie_detail)
    data = fetch_tmdb_api(f"movie/{movie_id}/credits")
    # pprint(data)
    cast = data.get('cast')

    cast_name = [item.get('original_name') for item in cast]
    print(cast_name)

except Exception as e:
    print('오류발생!')
    print(e)
```

```markdown
### 문제 8
현재 상영 중인 영화(now_playing) 데이터에서 다음 정보만 담긴 리스트를 만드세요.
단, 장르는 text로 이루어진 list입니다.

- title (영화 제목)  
- genres (장르)

hint) API 문서의 endpoint 항목을 참고하세요

```

```python
# detail에 너무 많은 요청 보내면 좋지 못함.

try:
    movies = get_movies()
    # pprint(movies[0])
    # for 문을 사용하기 전 내가 원하는 데이터를 바라볼 때 굳이 모든 데이터에서 대해서 볼 필요 없음.
    # movies[0]에 대해서 알아보고, 이걸 for문으로 확장

    movie_id = movies[0].get('id')
    

    # for movie in movies:
    #     # 해당 방법으로 genres의 이름들을 얻을 수 있지만, 너무 많은 요청이 필요하다.
    #     # => 다른 방법을 선택하겠다.
    #     movie_detail = get_movie_by_id(movie_id)
    #     pprint(movie_detail)

except Exception as e:
    print('오류발생!')
    print(e)
```

```python

try:
    movies = get_movies()

    genres_data = fetch_tmdb_api("genre/movie/list")
    genres = genres_data.get('genres')
    # print(genres)
    print("장르 길이", len(genres))
    my_movie_list = []

    # 얼마나 반복을 도는지 확인
    count = 0

    # movie = movies[0]
    for movie in movies:
        movie_genre_ids = movie.get('genre_ids')
        # print(movie_genre_ids)

        movie_genre_list = []
        for genre_id in movie_genre_ids:
            # 영화가 가지고 있는 각각의 id들에 대해서
            # genres랑 매칭을 하고 싶다.

            for genre in genres:
                if genre['id'] == genre_id:
                    movie_genre_list.append(genre['name'])
                    break # 장르를 발견하면 더이상 뒤를 확인할 필요 없음.
                count += 1
                    
        # print(movie_genre_list)
        my_movie_list.append((movie.get('title'), movie_genre_list))
    print(count)
    pprint(my_movie_list)

except Exception as e:
    print('오류발생!')
    print(e)

```

```python

try:
    movies = get_movies()

    genres_data = fetch_tmdb_api("genre/movie/list")
    genres = genres_data.get('genres')
    my_movie_list = []

    # 얼마나 반복을 도는지 확인
    count = 0
    
    # 영화가 가지고 있는 장르 id를 활용해서
    # api에서 가져온 장르 id - name을 한번에 가져오고 싶다.
    genre_mapping = {
        "1" : "액션",
        "2" : "스릴러"
    }
    # 처럼 만들면 genre_mapping.get(1) => 액션이 O(1)으로 나온다.

    # genres => genre_mapping 만들어야 겠다.

    genre_mapping = {}

    for genre in genres:
        genre_id = genre.get('id')
        genre_name = genre.get('name')

        genre_mapping[genre_id] = genre_name

        count += 1
    pprint(genre_mapping)

    my_movie_list = []

    for movie in movies:
        movie_genre_ids = movie.get('genre_ids')

        movie_genre_list = []
        for genre_id in movie_genre_ids:
            genre_name = genre_mapping[genre_id]
            movie_genre_list.append(genre_name)

            count += 1
        my_movie_list.append((movie.get('title'), movie_genre_list))

    print(count)

    # api 실시간이여서 여러분들이 돌릴때는 다를수도 있습니다.
    # 위의 코드 break 없을 때 : 1102
    # 위의 코드 break 있을 때 : 429
    # 이번 코드              : 77
    pprint(my_movie_list)
    
        

except Exception as e:
    print('오류발생!')
    print(e)

```

#### 공공데이터 실습

```markdown
### 공공 데이터 API

미세먼지(한국환경공단_에어코리아_대기오염정보)관련 API를 활용하여 다음 질문에 답하시오.

```

```python
import requests
from pprint import pprint
from dotenv import load_dotenv
import os
load_dotenv()

api_key = os.getenv('DUST_API_KEY')
```

```python
1. `시도별 실시간 측정정보 조회`에서  확인 가능한 시도 이름을 전부 작성하시오.
```

```python
# 정답은 코드가 아닙니다
전국, 서울, 부산, 대구, 인천, 광주, 대전, 울산, 경기, 강원, 충북, 충남, 전북, 전남, 경북, 경남, 제주, 세종

```

```markdown
2. `시도별 실시간 측정정보 조회`의 `서울`의 데이터에 대해, 초 미세먼지 농도가 가장 낮은 `stationName`을 찾으시오.
    1. `시도별 실시간 측정정보 조회`의 `제주`의 데이터에 대해, 초 미세먼지 농도가 가장 낮은 `stationName`을 찾으시오.
```

```python
# API 문서를 확인해보니 초 미세먼지 농도 -> pm25Value

try:
    url = "https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty?serviceKey=b82959989fe0a51f8b1a1ea2756b57be5bd5540e1af02f56c601aa120fdaf6cd&returnType=json&numOfRows=100&pageNo=1&sidoName=%EC%84%9C%EC%9A%B8&ver=1.0"

    response = requests.get(url)
    response.raise_for_status()

    json_data = response.json()

    response = json_data.get('response')
    body = response.get('body')
    items = body.get('items')

    # pprint(items)

    # print(min(items, key=lambda item : item.get('pm25Value')))
    # print([item.get('pm25Value') for item in items])

    # base로 첫 값을 넣자 라고 했었지만, 이 경우에는 '-'가 들어갈 수 있어서 안됨.
    # min_value = int(items[0].get('pm25Value'))
    min_value = float('inf')
    min_item = None
    for item in items:
        pm25Value = item.get('pm25Value')
        # print(min_value, pm25Value)
        # print(type(pm25Value))
        # 비교를 하려고 하니, pm25Value가 str이여서 비교가 불가능하다.
        
        if pm25Value == '-':
            continue
        if min_value > int(pm25Value):
            min_value = int(pm25Value)
            min_item = item
    print(min_value, min_item.get('stationName'))
except Exception as e:
    print(e)
    print(e.with_traceback())

```

```python
# timeout(504)이 발생한다 -> 500번대 -> 서버잘못.

# 1. 이따가 다시 시도한다. 근데 자동으로.

# 2. 이걸 json으로 저장해서 그걸 api인셈 치고 사용한다.
```

```python
# 1. 이따가 다시 시도한다. 근데 자동으로.

# for _ in range(횟수):
#     request 해봐

#     되? 그럼 굿

#     안되? 그럼 기다렸다가 다시 해.

import time
from urllib.parse import unquote

def get_dust_info(attempt = 3):

    for _ in range(attempt):
        try:
            url = "https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getCtprvnRltmMesureDnsty"

            params = {
                "serviceKey" : unquote(api_key),
                "returnType" : 'json',
                'numOfRows' : 100,
                'pageNo' : 1,
                'sidoName' : '서울',
                'ver' : 1.0
            }
            response = requests.get(url, params=params)

            # 504같은 정상적이지 않은 응답들을 raise합니다.
            response.raise_for_status()

            json_data = response.json()

            response = json_data.get('response')
            body = response.get('body')
            items = body.get('items')
            return items
        except Exception as e:
            print('오류발생 ㅠㅠ')
            print(e)
            time.sleep(1)
    

data = get_dust_info()

print(data)

```

```python
# 2. 이걸 json으로 저장해서 그걸 api인셈 치고 사용한다.
# 즉, 가짜 데이터를 만들어서 그거를 api인양 사용하면 됩니다.

# 1. api call을 합니다. 
    # (꼭 api call을 할 필요 없고, 그냥 내가 api 문서 보고 가짜로 만들어도 됩니다.)
# 2. 저장합니다.
    # json으로 저장해볼꺼에요
# 3. api 대신 저장된 녀석을 활용합니다.

# 데이터를 가지고 옵니다.
data = get_dust_info()

import json
# data.json을 'w', 즉 write하는 옵션으로 f라는 변수로 사용할꺼야.
with open('data.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=4)
    print('저장 완료!')
```

```python
# data = get_dust_info() # api 활용해서 데이터 가져오기.

# data.json을 'r', 즉 read하는 옵션으로 f라는 변수로 사용할꺼야.
with open("data.json", "r", encoding="utf-8") as f:
    json_data = json.load(f)

pprint(json_data)

# 아래처럼 둘을 합쳐 골라 사용할 수 있음.
def get_data(method="api"):
    if method == 'api':
        data = get_dust_info() # api 활용해서 데이터 가져오기.
    elif method == 'json':
        with open("data.json", "r", encoding="utf-8") as f:
            data = json.load(f)

    return data

```

```python
3. `시도별 실시간 측정정보 조회`의 `서울`의 데이터를 `stationName`으로 접근하기 쉬운 자료구조로 재구성하시오. 

```

```python
# data = get_dust_info()

# json_data를 불러왓다고 가정

# 각 stationName으로 쉽게 접근하기 위해서는 기존 데이터인 list보다는
# dictionary를 활용하면 쉽게 접근이 가능하다.
my_info = {}

for station in json_data:
    staion_name = station.get("stationName")
    my_info[staion_name] = station

# 관악구 자료 줘.
pprint(my_info['관악구'])
pprint(my_info['광진구'])
```

```python
4. `종로구`의 미세먼지 농도, 초미세먼지 농도의 1달치 데이터를 정리하시오.
```

```python

def get_dust_info_by_station_name(staion_name = '종로구', attempt = 3):

    for _ in range(attempt):
        try:
            url = "https://apis.data.go.kr/B552584/ArpltnInforInqireSvc/getMsrstnAcctoRltmMesureDnsty"

            params = {
                "serviceKey" : unquote(api_key),
                "returnType" : 'json',
                'numOfRows' : 1000, # 많이 잡아버리면 한번에 가져올 수 있다.
                # 정석은 numOfRows를 주는대로 사용하고, pageNo을 하나씩 늘려가면서 진행한다.
                'pageNo' : 1,
                'stationName' : staion_name,
                'dataTerm' : 'MONTH',
                'ver' : 1.0
            }
            response = requests.get(url, params=params)

            # 504같은 정상적이지 않은 응답들을 raise합니다.
            response.raise_for_status()

            json_data = response.json()

            # response = json_data.get('response')
            # body = response.get('body')
            # items = body.get('items')
            return json_data
        except Exception as e:
            print('오류발생 ㅠㅠ')
            print(e)
            time.sleep(1)

data = get_dust_info_by_station_name()

pprint(data)
```

강사님 왈 : 지금 공공데이터가  AI를 적용하면서 많이 더럽고 이상하게 만들어버렸다고 한다

ㅋㅋㅋ재미있다. 공공api를 사용할 때는 잘 사용해야겠다!!!

### 클래스 [이론]

```markdown
# 클래스
 : 객체를 생성하기 위한 설계도, 객체가 가지는 데이터(속성)와 기능(메서드)을 하나의 단위로
   정의한 사용자 정의 데이터 형식
* 객체 == 인스턴스
* 다른데서도 공유 및 서로 다른 데이터를 가질 수 있음
* 절차지향 -> 객체지향으로 넘어가고 있다
							(상호작용)
* 객체 : something이라고 생각하면 된다.

** 클래스 정의 및 인스턴스 생성

class Person :
		pass

p1 = Person()
p2 = Person()

class Person() :
		def __init__(self, name, age) : # 생성자
				self.name = name
				self.age = age
		
		def fucn(self) :
				기능
		
		
p1 = Person('김철수', 25)
p1.func()

print(p1.name) # 김철수

인스턴스 변수 / 메서드 

** 클래스 변수 : 클래스 내부에서 직접 선언, 클래스의 모든 인스턴스들이 공유하는 값

** 매직 메서드 : 이중 언더바, 내장 기능 수현할 때 사용, 특수 메서드

그 외 메서드는 그냥 참고만! 
```

```markdown
# 클래스 속성 및 메서드
* 데코레이터 : 

@property : 변수처럼 활용할 수 있게, 내부 데이터 노출 x, 가공된 값을 반환 및 읽기 전용 
            속성을 만들 때 사용
self._age = age -> 캡슐화 / 남들이 수정하지 못하게 할 때

@setter : 값을 넣는다. / 할당 전 유효성 검사를 수행하여 부적절한 데이터 저장 방지

** @property: 값을 읽을 때(Get) 동작합니다. (person.age)

** @age.setter: 값을 수정/대입할 때(Set) 동작합니다. (person.age = 20)

@classmethod : 클래스 변수에 접근, 클래스 상태 변경하는 로직 구현할때 사용

정적 메서드 : 인스턴스나 클래스 참조 없이 동작하는 메서드 -> @staticmethod
```

```markdown
# 클래스 상속
 : 정의된 클래스의 모든 속성, 메서드를 새로운 클래스가 물려받아 기능 확장 및 재사용
 
 부모클래스 : 기반, 상위 클래스
 자식클래스 : 이를 상속받는 클래스는 파생, 상속 클래스라고 함
 
 단일 상속 : 자식 클래스는 부모클래스에 정의된 것을 사용할 수 있다 
 
## 메서드 오버라이딩
 : 부모 클래스에 정의된 메서드를 자식 클래스에서 동일한 이름으로 다시 정의하여 
   동작을 변경하는 기술
   
#### super() 함수 : 부모 호출 가능 -> 덮어쓰기 전에 원본 호출 하는 것

## 다중 상속도 가능
```

```markdown
객체지향 프로그래밍 - 갭슐화/ 상속/ 추상화/ 다형성
```

### 클래스 실습

```markdown
# 파이썬 클래스 연습문제
```

```markdown
### 문제 1
이름(`name`)과 학번(`student_id`)을 속성으로 가지는 `Student` 클래스를 작성하세요.
1. `__init__` 생성자를 사용하여 두 속성을 초기화하세요.
2. 본인의 이름과 학번을 가진 인스턴스를 생성하여 각 속성값을 출력하세요.
```

```python
class Student() :
    def __init__(self, name, student_id):
        self.name = name
        self.student_id = student_id

stu =Student('철수', 1)
print(stu.name)
print(stu.student_id)
```

```markdown
### 문제 2
계산기 기능을 수행하는 `Calculator` 클래스를 만드세요.
1. 생성자에서 초기값 0을 가지는 `result` 인스턴스 변수를 생성합니다.
2. `add(self, num)` 메서드를 정의하여 호출 시 `result`에 값을 더합니다.
3. `subtract(self, num)` 메서드를 정의하여 호출 시 `result`에서 값을 뺍니다.
4. 메서드 호출 후 현재의 `result`를 확인하는 코드를 작성하세요.
```

```python
class Calculator() :
    def __init__(self):
        self.result = 0
    def add(self, num) :
        self.result += num
    def subtract(self, num) :
        self.result -= num
cal = Calculator()

cal.add(4)
print(cal.result)

cal.subtract(5)
print(cal.result)
```

```markdown
### 문제 3
매직 메서드를 사용하여 도서 정보를 관리하는 `Book` 클래스를 만드세요.
1. 생성자에서 제목(`title`)과 페이지 수(`pages`)를 입력받아 저장합니다.
2. `__str__` 메서드를 구현하여 `print()` 함수 호출 시 "제목: [제목]"이 출력되게 하세요.
3. `__len__` 메서드를 구현하여 `len()` 함수 호출 시 페이지 수가 반환되게 하세요.
```

```python
class Book() :
    def __init__(self, title, pages):
        self.title = title
        self.pages = pages
    def __str__(self):
        return f"제목 : [{self.title}]"
    def __len__(self):
        return self.pages

book = Book("책이름", 50)
print(book)
print(len(book))
```

```markdown
# 파이썬 클래스 상속 연습 문제
```

```markdown
### 문제 1
동물을 나타내는 `Animal` 클래스와 이를 상속받는 `Dog`, `Cat` 클래스를 작성하세요.
1. `Animal` 클래스는 생성자(`__init__`)에서 이름(`name`)을 저장하고, `move(self)` 메서드에서 "[이름]이 이동합니다."를 출력합니다.
2. `Dog` 클래스는 `Animal`을 상속받으며, 고유 메서드 `bark(self)`에서 "[이름]이 멍멍 짖습니다."를 출력합니다.
3. `Cat` 클래스는 `Animal`을 상속받으며, 고유 메서드 `mew(self)`에서 "[이름]이 냥냥 웁니다."를 출력합니다.
4. `Dog`, `Cat` 인스턴스를 생성하고 두 메서드를 모두 호출해보세요.
```

```python
class Animal() :
    def __init__(self, name):
        self.name = name
    def move(self) :
        return f"{self.name}을 이동합니다."

class Dog(Animal):
    def __init__(self, name):
        super().__init__(name)
    def bark(self):
        return f"{self.name}이/가 멍멍 짖습니다."

class Cat(Animal):
    def __init__(self, name):
        super().__init__(name)
    def mew(self):
        return f"{self.name}이/가 냥냥 웁니다."

dog = Dog("강아지")
cat = Cat("고양이")

print(dog.bark())
print(cat.mew())
```

```markdown
### 문제 2
전자제품을 나타내는 `Electronics` 클래스와 이를 상속받는 `Smartphone`, `Fan` 클래스를 작성하세요.
1. `Electronics` 클래스는 `brand`를 속성으로 가지며, `power_on(self)` 메서드에서 "[brand] 제품의 전원이 켜집니다."를 출력합니다.
2. `Smartphone` 클래스는 `super()`를 사용하여 부모의 `brand`를 초기화하고, 추가로 모델명(`model`)을 속성으로 가집니다.
3. `Smartphone` 클래스에서 `power_on(self)` 메서드를 오버라이딩하여, 부모의 기능을 먼저 실행한 후 "모델 [model]이 부팅을 시작합니다."를 추가로 출력하세요.
4. `Fan` 클래스에서 `power_on(self)` 메서드를 오버라이딩하여, 부모의 기능을 먼저 실행한 후 "바람이 붑니다"를 추가로 출력하세요.
```

```python
class Electronics():
    def __init__(self, brand):
        self.brand = brand
    def power_on(self):
        print(f"{self.brand} 제품의 전원이 켜집니다.")
class Smartphone(Electronics):
    def __init__(self, brand, model):
        super().__init__(brand)
        self.model = model
    def power_on(self):
        super().power_on()
        print(f"모델[{self.model}]이 부팅을 시작합니다.")
class Fan(Electronics):
    def __init__(self, brand):
        super().__init__(brand)
    def power_on(self):
        super().power_on()
        print("바람이 붑니다.")

phone = Smartphone("삼성", "갤럭시S")
fan = Fan("삼성")

phone.power_on()

fan.power_on()
    
```

```markdown
### 문제 3
두 개의 부모 클래스로부터 기능을 물려받는 자식 클래스를 작성하세요.
1. `Speaker` 클래스: `play_sound(self)` 메서드에서 "소리를 출력합니다."를 출력합니다.
2. `Camera` 클래스: `take_photo(self)` 메서드에서 "사진을 촬영합니다."를 출력합니다.
3. `SmartDevice` 클래스: `Speaker`와 `Camera`를 모두 상속받습니다.
4. `SmartDevice` 인스턴스를 생성하여 두 부모 클래스의 메서드를 모두 호출해보세요.
```

```python
class Speaker:
    def __init__(self):
        pass
    def play_sound(self):
        print("소리를 출력합니다.")
class Camera:
    def __init__(self):
        pass
    def take_photo(self):
        print("사진을 촬영합니다.")

class SmartDevice(Speaker, Camera):
    def __init__(self):
        super().__init__()
    def play_sound(self):
        return super().play_sound()
    def take_photo(self):
        return super().take_photo()

device = SmartDevice()
device.play_sound()
device.take_photo()
```