```markdown
#### 지난 학습 복습
class
인스턴스 변수/메서드
상속
등 저번주 복습!
```

#### 강사님 상속 문제 풀이

```markdown
### 문제 1
동물을 나타내는 `Animal` 클래스와 이를 상속받는 `Dog`, `Cat` 클래스를 작성하세요.
1. `Animal` 클래스는 생성자(`__init__`)에서 이름(`name`)을 저장하고, `move(self)` 메서드에서 "[이름]이 이동합니다."를 출력합니다.
2. `Dog` 클래스는 `Animal`을 상속받으며, 고유 메서드 `bark(self)`에서 "[이름]이 멍멍 짖습니다."를 출력합니다.
3. `Cat` 클래스는 `Animal`을 상속받으며, 고유 메서드 `mew(self)`에서 "[이름]이 냥냥 웁니다."를 출력합니다.
4. `Dog`, `Cat` 인스턴스를 생성하고 두 메서드를 모두 호출해보세요.
```

```python
class Animal:

    def __init__(self, name):
        self.name = name

    def move(self):
        print(f"{self.name}이 이동합니다.")

class Dog(Animal):

    def bark(self):
        print(f"{self.name}이 멍멍 짖습니다.")

class Cat(Animal):

    def mew(self):
        print(f"{self.name}이 냥냥 웁니다.")

dog = Dog('철수')
dog.move()
dog.bark()

cat = Cat('영희')
cat.move()
cat.mew()
# cat.bark()
```

```markdown
### 문제 2
전자제품을 나타내는 `Electronics` 클래스와 이를 상속받는 `Smartphone`, `Fan` 클래스를 작성하세요.
1. `Electronics` 클래스는 `brand`를 속성으로 가지며, `power_on(self)` 메서드에서 "[brand] 제품의 전원이 켜집니다."를 출력합니다.
2. `Smartphone` 클래스는 `super()`를 사용하여 부모의 `brand`를 초기화하고, 추가로 모델명(`model`)을 속성으로 가집니다.
3. `Smartphone` 클래스에서 `power_on(self)` 메서드를 오버라이딩하여, 부모의 기능을 먼저 실행한 후 "모델 [model]이 부팅을 시작합니다."를 추가로 출력하세요.
4. `Fan` 클래스에서 `power_on(self)` 메서드를 오버라이딩하여, 부모의 기능을 먼저 실행한 후 "바람이 붑니다"를 추가로 출력하세요.
```

```python
class Electronics:

    def __init__(self, brand):

        self.brand = brand

    def power_on(self):
        print(f"{self.brand}가 켜짐!")

class Smartphone(Electronics):
    def __init__(self, brand, model):
        super().__init__(brand)
        self.model = model

    def power_on(self):
        super().power_on()
        print(f"{self.model}이 부팅을 시작합니다.")

galaxy = Smartphone('삼성', 's26')
galaxy.power_on()
galaxy.model
```

```markdown
### 문제 3
두 개의 부모 클래스로부터 기능을 물려받는 자식 클래스를 작성하세요.
1. `Speaker` 클래스: `play_sound(self)` 메서드에서 "소리를 출력합니다."를 출력합니다.
2. `Camera` 클래스: `take_photo(self)` 메서드에서 "사진을 촬영합니다."를 출력합니다.
3. `SmartDevice` 클래스: `Speaker`와 `Camera`를 모두 상속받습니다.
4. `SmartDevice` 인스턴스를 생성하여 두 부모 클래스의 메서드를 모두 호출해보세요.
```

```python
class Speaker:
    def play_sound(self):
        print('소리가 나온다.')

class Camera:
    def take_photo(self):
        print('사진을 찍는다.')

class SmartDevice(Speaker, Camera):
    pass

sd = SmartDevice()
sd.play_sound()
sd.take_photo()
```

### 이론

```markdown
# 클래스 다형성
 : 동일한 이름의 메서드를 공유하면서 각자의 목적에 맞게 서로 다른 수행
   동일한 인터페이스를 통해 다양한 형태의 객체를 일관된 방식으로 제어, 코드의 유연성 확보

## 메서드 오버라이딩
 : 부모 클래스에 정의된 공통 메서드를 자식 클래서에서 재정의함, 다형성 구현

## 추상 클래스/다형성
 : 자식 클래스가 구현해야하는 것을 강제한다. 구조적 무결성
* 데코레이션을 달아 사용, abstractmethod / 하위 클래스에서 사용하지 않으면 오류가 남
```

```markdown
# Composition
 : 부분 - 전체 관계 형성 / 객체의 복합적인 기능을 구성
   즉, 어떻게 상호작용 할까라는 느낌
   하위 클래스에 기능을 직접 상속하는 대신, 필요한 기능을 가진 객체를 속성으로 포함 시킨다
   
class Person:
		def __init__(self)
				self.tool = None
class Computer:
		def use(selc):
				print('컴퓨터사용')

person = Person()
person.tool = Computer()
person.tool.use()

class Engine:
    def __init__(self, horsepower):
        self.horsepower = horsepower

    def start(self):
        # 엔진 시동 로직 구현
        return f"{self.horsepower}마력 엔진이 가동됩니다."

class Car:
    def __init__(self, model, horsepower):
        self.model = model
        # 외부 클래스의 인스턴스를 내부 속성으로 포함 (합성)
        self.engine = Engine(horsepower)

    def drive(self):
        # 포함된 객체의 기능을 활용하여 상위 기능을 수행
        engine_status = self.engine.start()
        return f"{self.model} 모델이 {engine_status} 주행을 시작합니다."

# Car 객체가 생성될 때 Engine 객체의 생명 주기가 결정됨
my_car = Car("전기차", 300)
print(my_car.drive())
```

```markdown
# 객체지향 프로그래밍 특징
1. 캡슐화
 * 캡슐화는 객체의 데이터와 데이터를 처리하는 메서드를 하나로 묶고, 내부 상태가 외부에서 
   함부로 변경되지 않도록 관리하는 특성
2. 상속 
 * 상속 보다는 컴포지션 활용
 * 상속은 기존 클래스의 속성과 메서드를 물려받아 새로운 클래스를 만드는 방식
3. 추상화
 * 추상화는 객체의 복잡한 내부 구현은 감추고, 외부에서 필요한 핵심 기능과 사용 
   방법만 드러내는 특성
4. 다형성
 * 다형성은 같은 방식으로 메서드를 호출해도, 전달된 객체에 따라 서로 다르게 동작하는 성질
```

#### 실습

```markdown
### 문제 1
추상 클래스(`Abstract Base Class`)를 사용하여 도형의 면적을 계산하는 구조를 작성하세요.
1. `abc` 모듈에서 `ABC`와 `abstractmethod`를 임포트합니다.
2. `Shape`라는 추상 클래스를 만들고, `get_area(self)`라는 추상 메서드를 정의하세요.
3. `Shape`를 상속받는 `Square`(변의 길이 `side` 속성) 클래스를 만드세요.
4. `Square` 클래스에서 `get_area()`를 오버라이딩하여 정사각형의 면적을 반환하도록 구현하세요.
```

```python
# 여기에 코드를 작성하세요.
from abc import ABC, abstractmethod

class Shape(ABC):
    @abstractmethod  
    def get_area(self):
        pass

class Square(Shape):
    def __init__(self,side : float):
        self.side : float = side
    def get_area(self) -> float:
        return self.side * self.side

square = Square(5)
square.get_area()

```

```markdown
### 문제 2
다형성(`Polymorphism`)을 활용하여 여러 동물들이 각자의 소리를 내는 프로그램을 작성하세요.
1. `Dog` 클래스와 `Cat` 클래스를 정의하고, 각각 `speak(self)` 메서드를 가집니다.
2. `Dog`은 "멍멍", `Cat`은 "야옹"을 출력하도록 `speak()` 메서드를 구현하세요.
3. 두 클래스의 인스턴스를 리스트에 담고, 반복문(for문)을 사용하여 리스트 내 모든 객체의 `speak()` 메서드를 호출하여 다형성이 작동하는지 확인하세요.
```

```python
# 여기에 코드를 작성하세요.
class Dog:
    def speak(self):
        print('멍멍')
class Cat:
    def speak(self):
        print('야옹')

animals = [Dog(), Cat()]

for animal in animals:
    animal.speak()

    # 덕타이핑. 
    #  : 프로그래밍에서 객체의 실제 이름이나 상속 관계보다, 그 객체가 가진 메서드와 속성이 무엇인가를 보고 타입을 판단하는 방식
```

```markdown
### 문제 3
합성(`Composition`) 관계를 활용하여 컴퓨터와 CPU의 관계를 구현하세요.
1. `CPU` 클래스를 정의하고, 모델명(`model`)을 속성으로 가집니다.
2. `Computer` 클래스를 정의하고, 생성자(`__init__`)에서 `CPU` 객체를 인자로 받아 인스턴스 변수에 할당(Composition)하세요.
3. `Computer` 클래스에 `show_info(self)` 메서드를 만들어, 장착된 CPU의 모델명을 포함하여 "컴퓨터 사양: [CPU 모델명]"을 출력하세요.
```

```python
# 여기에 코드를 작성하세요.
# 여기에 코드를 작성하세요.

class CPU:
    def __init__(self, model : str):
        self.model : str = model

class Computer:
    def __init__(self, cpu : CPU):
        self.cpu : CPU = cpu

    def show_info(self):
        print(f'컴퓨터 사양 : {self.cpu.model}')
        

cpu = CPU("i5-1999")
computer = Computer(cpu)

computer.show_info()
```

### 타입 힌트!

```markdown
# 파이썬 타입 힌트(Type hinting)

타입 힌트는 코드의 변수, 함수 매개변수, 반환값에 기대되는 데이터 타입을 명시적으로 선언하는 기능이다. 이는 프로그램 실행 시 강제성을 가지지는 않으나 정적 분석 도구와 개발 환경의 지원을 통해 코드의 안정성을 높인다.
```

```markdown
### 기본 변수 타입 힌트

변수 이름 뒤에 콜론을 작성하고 데이터 타입을 명시한다. 값은 할당 연산자를 통해 지정한다.
```

```python
user_count: int = 10 # 정수형 변수 선언
user_name: str = "관리자" # 문자열 변수 선언
is_connected: bool = False # 불리언 변수 선언
average_score: float = 85.5 # 실수형 변수 선언
```

```markdown
### 함수 매개변수 및 반환 타입 힌트

함수의 각 매개변수 뒤에 콜론으로 타입을 기재하고 화살표 기호를 사용하여 함수가 반환하는 데이터 타입을 정의한다.
```

```python
def calculate_total(price: int, count: int) -> int:
    # 정수형 매개변수 두 개를 전달받아 정수형 결과를 반환함
    return price * count

def calculate_total2(price, count):
    # 정수형 매개변수 두 개를 전달받아 정수형 결과를 반환함
    return price * count

def greet(name: str) -> str:
    # 문자열 매개변수를 전달받아 문자열 결과를 반환함
    return "접속자: " + name
```

```markdown
### 리스트 및 딕셔너리 타입 힌트

내장된 자료구조 내부에 포함될 요소들의 타입을 대괄호를 사용하여 상세히 정의한다.
```

```python
# 정수들로 구성된 리스트
numbers: list[int] = [1, 2, 3, 4, 5]

# 문자열 키와 정수형 값으로 구성된 딕셔너리
user_scores: dict[str, int] = {"kim": 90, "lee": 80}
```

```markdown
### 합집합 타입 힌트

하나의 변수가 여러 데이터 타입 중 하나를 가질 수 있을 때 수직선 기호를 사용하여 모든 가능성을 명시한다. (python 3.10 이상)

이전 버전에서는 `Union`을 활용하여 합집합을 나타내었다.
```

```python
def process_id(user_id: int | str) -> None:
    # 매개변수 user_id는 정수 또는 문자열 모두 허용됨
    # 반환값이 없는 경우 None으로 명시함
    print(f"아이디 확인: {user_id}")

process_id(101)
process_id("user_01")
```

```python
def find_user_name(user_id: int) -> str | None:
    if user_id == 1:
        return "관리자"

    return None

def print_message(message: str) -> None:
    """메시지를 출력합니다."""
    print(message)
```

```markdown
### 클래스 인스턴스 타입 지정

정의된 클래스 이름을 타입 힌트로 사용하여 해당 클래스의 인스턴스만 매개변수로 받거나 반환하도록 명시한다.
```

```python
class User:
    def __init__(self, name: str, age: int):
        self.name = name
        self.age = age

def print_user_info(user: User) -> None:
    # User 클래스의 인스턴스를 매개변수로 전달받음
    print(f"이름: {user.name}, 나이: {user.age}")

new_user = User("홍길동", 30)
print_user_info(new_user)
```

```markdown
### 다형성과 composition에서의 타입 힌트
클래스를 타입 힌트로 사용하면 특정 클래스 또는 그 하위 클래스의 인스턴스를 매개변수로 받을 수 있다.
```

```python
class Notifier:
    def send(self, message: str) -> None:
        raise NotImplementedError

class EmailNotifier(Notifier):
    def send(self, message: str) -> None:
        print(f"[이메일] {message}")

class SmsNotifier(Notifier):
    def send(self, message: str) -> None:
        print(f"[문자] {message}")

class OrderService:
    def __init__(self, notifier: Notifier):
        # Notifier 객체를 전달받아 사용하는 합성 방식
        self.notifier: Notifier = notifier

    def complete_order(self, order_id: int) -> None:
        self.notifier.send(f"주문 {order_id}번이 완료되었습니다.")

email_service = OrderService(EmailNotifier())
email_service.complete_order(101)

sms_service = OrderService(SmsNotifier())
sms_service.complete_order(102)
```

```markdown
# 독스트링
독스트링은 모듈, 함수, 클래스 또는 메서드 정의의 첫 번째 문장으로 작성되는 문자열 이다. 

코드의 의도, 사용법, 매개변수 및 반환값 등을 설명하여 코드의 가독성과 유지보수성을 높이며, 단순한 주석(#)과 달리, 파이썬 런타임에서 객체의 __doc__ 속성으로 저장되어 접근이 가능하다.

"""세 개의 큰따옴표"""를 사용하여 표현한다.

참고) https://google.github.io/styleguide/pyguide.html#383-functions-and-methods
```

```python
def calculate_area(radius: float) -> float:
    """원의 반지름을 입력받아 면적을 계산하여 반환함.

    Args:
        radius : 원의 반지름.
    Returns:
        계산된 원의 면적.
    """
    import math
    return math.pi * (radius ** 2)

print(calculate_area.__doc__)  # 런타임에서 확인 가능
help(calculate_area)
```

#### 심화 실습

```markdown
OOP 기반 스마트 홈 설계 및 구현
1. 요구 사항
[스마트 허브(SmartHub) 설계]
허브는 이름과 지원하는 연결 방식(Protocol)을 속성으로 가집니다.
허브는 여러 개의 기기(Device)를 저장할 수 있는 리스트를 가집니다.
기능: 기기_등록(), 전체_가동(), 전체_중지().
 

[기기(Device) 설계 - 추상 클래스]
모든 기기는 이름, 제조사, 연결 방식 속성을 가집니다.
기기 생성 시 이름과 연결 방식이 지정되어야 합니다.
기능: 작동(), 상태확인() 정지()(추상 메서드).
구현 클래스: 스마트전등(SmartLight), 에어컨(AirConditioner)을 생성하세요.
 

[연결 방식(Protocol) 설계 - 추상 클래스]
연결 방식은 통신_시작() 기능을 가집니다.
구현 클래스: WiFiProtocol, ZigbeeProtocol, BluetoothProtocol을 생성하세요.
각 프로토콜은 통신_시작() 호출 시 "WiFi로 연결을 시도합니다", "Zigbee망을 구성합니다" 등 고유의 메시지를 출력합니다.
 

2. 핵심 로직
합성(Composition)과 호환성 검사
허브의 기기_등록(기기객체) 메서드 실행 시:
제한 사항: 등록하려는 기기의 연결 방식이 허브가 지원하는 연결 방식과 일치하지 않으면 "호환되지 않는 연결 방식입니다"라는 에러를 출력하고 등록되지 않아야 합니다.
일치할 경우에만 허브의 기기 리스트에 추가됩니다.
 

다형성을 활용한 제어 시스템
허브의 전체_가동()을 호출하면:
등록된 모든 기기를 순회하며,
먼저 기기의 프로토콜 객체가 가진 통신_시작()을 호출하고,
이어서 각 기기의 작동() 메서드를 실행합니다.
 

3. 테스트 시나리오 (Test Case)
허브 생성: Zigbee 방식만 지원하는 '샤오미 허브' 생성.
기기 생성 1: WiFi 방식을 사용하는 '필립스 전등' 생성.
기기 생성 2: Zigbee 방식을 사용하는 '이케아 전등' 생성.
등록 테스트 (실패): '샤오미 허브'에 '필립스 전등(WiFi)' 등록 시도 -> 실패 메시지 확인.
등록 테스트 (성공): '샤오미 허브'에 '이케아 전등(Zigbee)' 등록 시도 -> 성공.
가동 테스트: '샤오미 허브'의 전체_가동() 호출 -> 통신 시작 메시지와 기기 작동 메시지가 순차적으로 출력되는지 확인.
```

```python
from abc import ABC, abstractmethod
from device import Device
from protocol import Protocol

class SmartHub:
    def __init__(self, name, protocol : Protocol, devices : list[Device] =[]):
        self.name : str = name
        self.protocol : Protocol = protocol
        self.devices : list[Device] = devices

    def register_device(self, device):
        if isinstance(device.protocol, type(self.protocol)):
            self.devices.append(device)
            print("연결 성공")
        else :
            print("연결실패")

    def run_all(self):
        for device in self.devices:
            self.protocol.start_connection()
            device.run()

    def stop_all(self):
         for device in self.devices:
            self.protocol.start_connection()
            device.stop()
```

```python
from abc import ABC, abstractmethod

class Protocol(ABC):
    @abstractmethod
    def start_connection(self):
        pass

class WiFiProtocol(Protocol):
    def start_connection(self):
        print("WiFi 연결")

class ZigbeeProtocol(Protocol):
    def start_connection(self):
        print("Zigbee 연결")

class BluetoothProtocol(Protocol):
    def start_connection(self):
        print("블루투스 연결")
```

```python
from abc import ABC, abstractmethod
from protocol import Protocol

class Device(ABC):
    def __init__(self, name : str, company : str, protocol : Protocol):
        self.name : str = name
        self.company : str = company
        self.protocol : Protocol = protocol

    @abstractmethod
    def run(self):
        pass

    @abstractmethod
    def check_status(self):
        pass

    @abstractmethod
    def stop(self):
        pass

class SmartLight(Device):
    def __init__(self, name, company, protocol):
        super().__init__(name, company, protocol)
        self.status = False

    def run(self):
        if self.status:
            print("전등 이미 켜져있음")
        else :
            self.status = True
            print("전등 킴!")
    
    def check_status(self):
        if self.status:
            print("전등 켜져있음")
        else :
            print("전등 꺼져있음")

    def stop(self):
        if not self.status:
            print("전등 이미 꺼져있음")
        else :
            self.status = False
            print("전등 끔!!")

class AirConditioner(Device):
    def __init__(self, name, company, protocol):
        super().__init__(name, company, protocol)
        self.status = False
        
    def run(self):
        if self.status:
            print("에어컨 이미 켜져있음")
        else :
            self.status = True
            print("에어컨 킴!")
    
    def check_status(self):
        if self.status:
            print("에어컨 켜져있음")
        else :
            print("에어컨 꺼져있음")

    def stop(self):
        if not self.status:
            print("에어컨 이미 꺼져있음")
        else :
            self.status = False
            print("에어컨 끔!!")
```

```python
from device import Device, SmartLight, AirConditioner
from protocol import Protocol, ZigbeeProtocol, WiFiProtocol, BluetoothProtocol
from smartHub import SmartHub

zigbee = ZigbeeProtocol()
wifi = WiFiProtocol()
bluetooth = BluetoothProtocol()

sh = SmartHub("허브", zigbee)

light = SmartLight("전등임", "샤오미??", zigbee)

sh.register_device(light)

sh.run_all()
```

```markdown
OOP 기반 게임 캐릭터 시스템 설계 및 구현
게임의 캐릭터 운영 규칙
1. 캐릭터는 이름과 역할을 가지고 태어납니다.
우리 게임의 모든 캐릭터는 이름표를 달고, 자기가 무엇을 하는 사람인지 역할을 정해서 태어납니다. 이름이 없거나 역할이 정해지지 않은 상태로는 게임 세상에 발을 들일 수 없습니다.

2. 어떻게 싸우는지는 역할에 따라 결정됩니다.
모든 캐릭터에게 똑같이 공격하라고 명령을 내려도, 그 사람이 맡은 역할에 따라 행동이 달라집니다.

전사 역할을 맡은 사람에게 명령하면 본능적으로 검을 휘두르며 나갑니다.
마법사 역할을 맡은 사람에게 명령하면 자연스럽게 마법을 주문처럼 외우며 나갑니다.
캐릭터가 손에 무엇을 들었느냐와 상관없이, 그 사람이 평생 배워온 싸움의 방식이 나오는 것입니다.
3. 도구(무기)는 아무나 들 수 없습니다.
이 세상에는 검이나 지팡이 같은 다양한 도구들이 있습니다. 각 도구에는 그 도구만이 가진 필살기가 하나씩 숨겨져 있죠. 하지만 아무나 이 도구를 집어 들 수 있는 건 아닙니다.

각 역할마다 이런 종류의 도구만 써도 좋다는 허가 리스트가 있습니다.
만약 전사에게 지팡이를 쥐여주려고 하면, 전사는 나는 이런 건 배운 적이 없어서 못 써요라며 거절합니다. 오직 허락된 도구인 검을 줄 때만 기쁘게 받아들여 손에 쥡니다.
4. 무기를 들어야만 필살기를 씁니다.
캐릭터에게 무기에 숨겨진 기술을 써보라고 시켰을 때의 상황입니다.

손에 무기가 있다면: 그 무기에 적혀 있는 화려한 필살기를 사용합니다. 예를 들어 검이라면 강력한 베기 같은 기술이 나갑니다.
맨손이라면: 지금 내 손에 든 게 없는데 무엇을 쓰나요라고 당황해합니다. 즉, 무기를 제대로 장착했을 때만 가능한 행동입니다.
상황 예시
탄생: 아더라는 이름의 전사가 태어났습니다.
도구 장착 시도 1: 아더에게 마법사가 쓰는 지팡이를 줬더니, 아더는 이건 내 역할에 맞지 않아요라며 거절합니다. 아더의 손은 여전히 비어있습니다.
도구 장착 시도 2: 이번엔 검을 줬습니다. 아더는 이건 제가 잘 다루는 거죠라며 꽉 쥡니다.
기본 공격: 아더에게 공격해라고 하자, 전사답게 검을 휘두르며 달려나갑니다.
필살기 사용: 아더에게 무기 기술 써봐라고 하자, 손에 든 검의 필살기인 연속 베기를 선보입니다.
```

```python
from python.python_class.game.job import Job
from python.python_class.game.weapon import Weapon
class Character:

    def __init__(self, name, job: Job):
        self.name = name
        self.job: Job = job
        self.weapon: Weapon | None = None

    def attack(self):
        self.job.attack()

    def equip_weapon(self, weapon: Weapon):
        # 허가 리스트에 있어?
        if type(weapon) in self.job.allow_weapon_list:
            self.weapon = weapon
            print('장비 착용 완료!')
        # 없으면
        else:
            print("장비할 수 없습니다.")
    # def use_skill(self):

    def use_skill(self):
        if self.weapon is not None:
            print('스킬 사용')
            self.weapon.skill()
        else:
            print('스킬 사용 불가.')
```

```python
from abc import ABC, abstractmethod
from python.python_class.game.weapon import Weapon, Staff, Sword
class Job(ABC):

    @abstractmethod
    def attack(self):
        pass

class Warrior(Job):
    allow_weapon_list: set[Weapon]= { Sword, }

    def attack(self):
        print('검을 휘두른다!')

class Magicion(Job):
    allow_weapon_list: set[Weapon]= { Staff, }

    def attack(self):
        print('마법 발사!')

```

```python
from abc import ABC, abstractmethod
class Weapon(ABC):

    @abstractmethod
    def skill(self):
        pass

class Sword(Weapon):

    def skill(self):
        print('강력한 베기')

class Staff(Weapon):
    
    def skill(self):
        print('강력한 마법)')
```

```python
from python.python_class.game.chartacter import Character
from python.python_class.game.job import Warrior, Magicion
from python.python_class.game.weapon import Sword, Staff
# 캐릭터 만들기(이름, job)

warrior = Warrior()
magician = Magicion()

arther = Character('아서', warrior)
# 무기 쥐어주기

arther.attack()

arther.job = magician

arther.attack()

# 공격하기

# 장비 착용하기

staff = Staff()

arther.equip_weapon(staff)
# 스킬사용하기

arther.use_skill()

# 무기를 착용한 상태에서 직업을 바꿔버려요.
# 그럼 staff를 착용한 상태에서 warrior로 직업이 바뀝니다.
# job에 대한 setter에다가 다시 weapon이 해당 job에 맞는지를 체크하는 로직이
# 들어가야 할겁니다.
```

좀더 쉽게!

```python
from job import Job
from weapon import Weapon

class Character:
    def __init__(self, name, job : Job):
        self.name = name
        self.job = job
        self.weapon = None

    def attack(self):
        self.job.attack()

    def equip_weapon(self, weapon : Weapon):
        if self.job.can_use_weapon(weapon):
            self.weapon = weapon
            print(f"{self.name}이 무기 장착 완료")
        else :
            print(f"{self.name} 직업과 다른 무기 장착 불가능!")

    def use_weapon_skill(self):
        if self.weapon is None:
            print(f"장착한 무기가 없습니다.")
        else:
            self.weapon.use_skill()
```

```python
from abc import ABC, abstractmethod
from weapon import Weapon, Sword, Staff

class Job(ABC):
    allowed_weapons = []

    @abstractmethod
    def attack(self):
        pass

    def can_use_weapon(self, weapon : Weapon):
        return type(weapon) in self.allowed_weapons
    
class Warrior(Job):
    allowed_weapons = [Sword]
    def attack(self):
        print("검을 휘두름")

class Mage(Job):
    allowed_weapons = [Staff]
    def attack(self):
        print("지팡이를 휘두름")

```

```python
from abc import ABC, abstractmethod

class Weapon(ABC):
    @abstractmethod
    def use_skill(self):
        pass

class Sword(Weapon):
    def use_skill(self):
        print("강력한 베기")

class Staff(Weapon):
    def use_skill(self):
        print("강력하게 지팡이 휘두르기")
```

```python
from character import Character
from job import Warrior, Mage
from weapon import Weapon, Sword, Staff

warrior = Warrior()
mage = Mage()

arthur = Character("아더", warrior)
merlin = Character("멀린", mage)

arthur.attack()
merlin.attack()

sword = Sword()
staff = Staff()

sword.use_skill()
staff.use_skill()

print(warrior.can_use_weapon(sword))
print(mage.can_use_weapon(staff))

arthur.equip_weapon(staff)
print(arthur.weapon)

arthur.use_weapon_skill()

arthur.equip_weapon(sword)
# print(arthur.weapon)

arthur.use_weapon_skill()

```

아직 많이 헷갈리니 주말 간에 한번더 해보자!!!!!