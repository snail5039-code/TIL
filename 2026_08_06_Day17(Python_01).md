```python
### Python - 언어
   (프로그래밍)

x = 3 변수
1 + x = 4
집어넣는 것을 할당이라고 한다.

names = [jun, aiden, ken] <- list(리스트)

person {name : jun, age:30, gender:m} -> dictionary
       (key)  (value)

data : {
	'이름' : ['jun', 'aiden', 'ken'],
	'나이' : [10, 20, 30],
	'성별' : ['남', '남', '남']
}
```

```python
## Python 설치

### pyenv

- pyenv는 한 대의 컴퓨터에서 여러 버전의 파이썬을 자유롭게 설치하고 전환할 수 있게 해주는 파이썬 버전 관리 도구이다.

<br>

<details> 

<summary>
Window
</summary>

### pyenv 설치

pyenv-win/docs/installation.md at master · pyenv-win/pyenv-win

1. window키 → PowerShell 검색 → 관리자 권한으로 실행
2. 아래 명령어 입력 후 PowerShell 종료
    
    ```bash
    git clone https://github.com/pyenv-win/pyenv-win.git "$env:USERPROFILE\.pyenv"
    ```
    
    ```bash
    [System.Environment]::SetEnvironmentVariable('PYENV', $env:USERPROFILE + "\.pyenv\pyenv-win\", "User")
    [System.Environment]::SetEnvironmentVariable('PYENV_ROOT', $env:USERPROFILE + "\.pyenv\pyenv-win\", "User")
    [System.Environment]::SetEnvironmentVariable('PYENV_HOME', $env:USERPROFILE + "\.pyenv\pyenv-win\", "User")
    ```
    
    ```bash
    [System.Environment]::SetEnvironmentVariable('PATH', $env:USERPROFILE + "\.pyenv\pyenv-win\bin;" + $env:USERPROFILE + "\.pyenv\pyenv-win\shims;" + [System.Environment]::GetEnvironmentVariable('PATH', "User"), "User")
    ```
    
3. 권한 문제가 발생한다면 아래 명령어 입력 후 PowerShell 종료. 이후 2번 명령어 입력
    
    ```jsx
    Set-ExecutionPolicy RemoteSigned -Scope CurrentUser -Force
    ```
    
- bash에서 인식이 안된다면
    
    ```bash
    cat >> ~/.bashrc << 'EOF'
    # pyenv-win
    export PYENV_ROOT="$HOME/.pyenv/pyenv-win"
    export PATH="$PYENV_ROOT/bin:$PYENV_ROOT/shims:$PATH"
    EOF
    ```
    
### python 설치

1. 파이썬 3.13 버전 설치
PowerShell 에 아래 명령어 입력
    
    ```bash
    pyenv install 3.13
    ```
    
2. 3.13 버전의 python 설정
PowerShell 에 아래 명령어 입력
    
    ```bash
    pyenv global 3.13
    ```
    
3. 파이썬 버전 확인
    
    ```bash
    python --version
    ```
    
</details> 

<br>

<details> 

<summary>
Mac
</summary>

### Homebrew 설치

homebrew는 macOS 운영 체제의 소프트웨어 패키지 관리 시스템이다.

1. terminal 열기
    -  spotlight에서 terminal 검색
2. 아래 명령어 실행
    
    ```jsx
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    ```
    

### pyenv 설치

1. terminal 열기
    -  spotlight에서 terminal 검색

1. 아래 명령어 실행
    
    ```jsx
    brew install pyenv
    ```
    
    ```jsx
    pyenv init --install
    ```
    

### python 설치

1. 파이썬 3.13 버전 설치
terminal에 아래 명령어 입력
    
    ```bash
    pyenv install 3.13
    ```
    
2. 3.13 버전의 python 설정
terminal 에 아래 명령어 입력
    
    ```bash
    pyenv global 3.13
    ```
    
3. 파이썬 버전 확인
    
    ```bash
    python --version
    ```
    
</details> 

<br>

## VScode 설정

좌측의 `Extensions`에서 

- python 검색 후 설치
    
    !파이썬
    

- jupyter 검색 후 설치
    
    !주피터

```

### ipynb

 : 마크다운, 코드랑 하나에 파일 안에 저장된 것 

```python
## 할당 
 : 오른쪽 것을 왼쪽에 집어넣는다
* 변수 = data
print() 함수, function(기능)

데이터 
 : 문자, 숫자, 불린
 
type() : 데이터의 타입을 알 수 있다.

변수
1. 데이터에 의미를 부여
2. 코드의 재사용, 가독성을 높여준다

식별자 : 변수의 이름
* 알파벳, 언더바, 숫자로 구성
* 첫글자 숫자 x
* 대소문자 구별
* 띄어쓰기가 필요한 경우 언더바로 구분
* 예약어로 지을 수 없다.

리터럴 : 데이터 값(오른쪽 것)

마지막 것만 기록되는 것 : 재할당
동시할당 : 존재한다는 것만 알면 된다

교육에서는 변수는 변해서, 상수는 항상 존재해서 왠만하면 변수를 상수처럼 사용할 것이다.
```

```markdown
자료형
- 글자, 숫자, 불린(T, F)
* 숫자 
 : 정수형(int), 실수형(float)

* 문자
 : 한글, 알파벳 문자들의 집합, '', ""로 감싸 표기
 * 프로젝트 내에서 정하면 된다.
 * 공백과 빈 문자열도 하나의 문자열로 취금

이스케이프 시퀸스
 : 백슬래시를 사용하여 특수한 문자를 표현

| 시퀀스 | 설명 | 예시 |
|:---:|:---|:---|
| `\'` | 작은따옴표 표현 | `'\'이렇게\''` |
| `\"` | 큰따옴표 표현 | `"\"이렇게\""` |
| `\\` | 백슬래시 표현 | `"\\"` |
| `\n` | 줄바꿈(Next line) | `"Hello\nPython"` |
| `\t` | 탭(Tab) | `"Name:\tKyle"` |

불린형
- 참(True)과 거짓(False)을 뜻함

명시적 형변환
* int() : 정수형으로 변환
 : 문자 -> 정수
   실수 -> 정수

* float() : 실수형으로 변환
 : 문자 -> 실수
   정수 -> 실수

* str() : 문자열로 변환
```

```markdown
## 연산자
### 산술 연산자
* +, *, //, **, -, /, % 가 있다

ZeroDivisonError : 정수 0으로 다른 수를 나누게 되면 에러 발생

* 복합연산자는 읽어보기

* 비교연산자

* 논리 연산자
| 논리 연산자 | 설명 |
|:---:|:---|
| `a and b` | a와 b가 모두 True이면 True |
| `a or b` | a와 b 중 하나라도 True이면 True |
| `not a` | a가 True이면 False, False이면 True |
```

```markdown
## 입력과 출력
* 내장함수 
- input() : 입력
- print() : 출력

## 조건문
내가 만약 부자라면
치킨을 먹을텐데

rich - boolean
부자이거나 아니거나.
if rich :
	eat chiken

if 조건문 - boolean :
	위에가 True면 여기가 실행된다.

여기는 위의 if문과 전혀 상관이 없다.

if else.
if 조건문 :
	실행할 무언가
else :
	거짓이면 실행할 무언가
	
내가 돈이 000 만큼 있어.

만약 돈이 1000보다 많다면 츄파츕스 사먹을래
막약 돈이 10000보다 많다면 김밥 사먹을래
만약 돈이 30000보다 많다면 치킨 사먹을래

사먹는다 -> print()

money = int(input())
if money > 1000 :
    print("츄파츕스 사먹을래")
elif money > 10000 :
    print("김밥 사먹을래")
elif money > 30000 :
    print("치킨 사먹을래")
else :
    print("사먹을게 없네")

if money >= 1000 :
    if money >= 10000 :
        if money >= 30000 :
            print("치킨 사먹을래")
        else :
            print("김밥 사먹을래")
    else :
        print("츄파츕스 사먹을래")
else :
	print('못 사먹음ㅠ')
```

```markdown
## 입력과 출력
* 내장함수 
- input() : 입력
- print() : 출력

## 조건문
내가 만약 부자라면
치킨을 먹을텐데

rich - boolean
부자이거나 아니거나.
if rich :
	eat chiken

if 조건문 - boolean :
	위에가 True면 여기가 실행된다.

여기는 위의 if문과 전혀 상관이 없다.

if else.
if 조건문 :
	실행할 무언가
else :
	거짓이면 실행할 무언가
	
내가 돈이 000 만큼 있어.

만약 돈이 1000보다 많다면 츄파츕스 사먹을래
막약 돈이 10000보다 많다면 김밥 사먹을래
만약 돈이 30000보다 많다면 치킨 사먹을래

사먹는다 -> print()

money = int(input())
if money > 1000 :
    print("츄파츕스 사먹을래")
elif money > 10000 :
    print("김밥 사먹을래")
elif money > 30000 :
    print("치킨 사먹을래")
else :
    print("사먹을게 없네")

if money >= 1000 :
    if money >= 10000 :
        if money >= 30000 :
            print("치킨 사먹을래")
        else :
            print("김밥 사먹을래")
    else :
        print("츄파츕스 사먹을래")
else :
	print('못 사먹음ㅠ')
	
#### 조건은 큰 값부터 처리하면 좋다고 함!
```

### 실습

```markdown
### 문제 1
본인의 이름, 키, 그리고 면허 소지 여부를 나타내는 데이터를 각각 변수에 할당하세요.
```

```python
name = "박의혁"
high = 170
concession = True
# 우리는 스네이크 케이스 쓸거야
# 명사 위주로 쓰되, 등등...
```

```markdown
### 문제 2
다음과 같은 변수들이 선언되어 있습니다.
```python
v1 = 123
v2 = "123"
v3 = "12.3"
v4 = 12.3
v5 = "True"
v6 = True
```
각 변수가 어떤 데이터 타입인지 확인하세요
```

```python
v1 = 123
v2 = "123"
v3 = "12.3"
v4 = 12.3
v5 = "True"
v6 = True

print(type(v1))
print(type(v2))
print(type(v3))
print(type(v4))
print(type(v5))
print(type(v6))
```

```markdown
### 문제 3
두 정수 `num1 = 15`, `num2 = 20`이 주어졌을 때, 다음 조건을 모두 만족하는 논리 연산식을 작성하고 그 결과를 출력하세요.
1. `num1`이 10보다 크다.
2. `num2`가 30보다 작거나 같다.
3. 위 두 조건이 모두 참(True)인지 확인한다.
```

```python
num1 = 15
num2 = 20

# 여기에 논리 연산식을 작성하세요.
print(num1 > 10)
print(num2 <= 30)
```

```markdown
### 문제4
논리 연산자를 사용하여 다음 조건을 만족하는 식을 작성하세요.

score가 80점 이상이고 100점 이하이면 True, 아니면 False를 출력하세요.

```

```python
score = 85

print(80 <= score <= 100)
print(score >= 80 and score <= 100)
```

```markdown
### 문제 5
물건의 가격이 price이고, 낸 돈이 money일 때, 거스름돈에서 500원 동전의 개수와 100원 동전의 개수를 구하는 프로그램을 작성하세요. 
(단, 가격은 항상 100원 단위이며 money > price라고 가정합니다.)

예: price = 3200, money = 5000 -> 500원: 3개, 100원: 3개
```

```python
price = 3200
money = 5000

change = money - price

change500 = change // 500

change100 = change % 500 // 100

print(f"500원 : {change500}개, 100원 : {change100}개")
```

```markdown
### 문제 1
나이를 입력받아 다음 조건에 해당하면 "무료 입장입니다"를 출력하고, 그렇지 않으면 "유료 입장입니다"를 출력하세요.
- 조건: 나이가 7세 이하이거나 65세 이상인 경우
```

```python
age = int(input())

if 65 <= age or age <= 7 :
    print("무료 입장입니다.")
else :
    print("유로 입장입니다.")
```

```markdown
### 문제 2
수를 입력받아 해당 수가 짝수이면 "짝수입니다"를, 홀수이면 "홀수입니다"를 출력하는 코드를 작성하세요.
```

```python
num = int(input())

if num % 2 == 0 :
    print("짝수입니다.")
else :
    print("홀수입니다.")

# 이런 방식도 가능하다 True/False 이용
if num % 2 :
    print("홀수")
else :
    print("짝수")
```

```markdown

### 문제 3
점수를 입력받아 다음 조건에 따라 학점을 출력하는 프로그램을 작성하세요.
- 90점 이상: A
- 80점 이상 90점 미만: B
- 70점 이상 80점 미만: C
- 70점 미만: F
```

```python
score = int(input())

if score >= 90 :
    print("A")
elif score >= 80 :
    print("B")
elif score >= 70 :
    print("C") 
else :
    print("F")
```

```markdown
### 문제 4
아이디(`user_id`)와 비밀번호(`user_pw`)를 입력받습니다.
- 아이디가 `"admin"`이고 비밀번호가 `"1234"`인 경우: "로그인 성공" 출력
- 아이디가 `"admin"`이지만 비밀번호가 틀린 경우: "비밀번호가 틀렸습니다" 출력
- 아이디가 틀린 경우: "존재하지 않는 아이디입니다" 출력
```

```python
user_id = input()
user_pw = input()

if user_id == "admin" and user_pw == "1234" :
    print("로그인 성공")
elif user_id == "admin" and user_pw != "1234" :
    print("비밀번호가 틀렸습니다.")
else :
    print("존재하지 않는 아이디입니다.")

if user_id == "admin": 
    if user_pw == "1234": 
        print("로그인 성공")
    else: 
        print("비밀번호가 틀렸습니다.") 
else: 
    print("존재하지 않는 아이디입니다.") 

# 단, 요즘은 보안 관련해서 실제 프로덕트에서는 비밀번호가 틀렸다고 나오지 않습니다.
# 아이디 혹은 비밀번호가 틀렸다고 퉁칩니다.
```

```markdown
### 문제 5
사용자로부터 정수 하나를 입력받아 변수 `num`에 저장하세요. 다음 조건에 따라 결과를 출력하세요.
- 양수라면: "양수입니다."
- 음수라면: "음수입니다."
- 0이라면: "0입니다."
```

```python
num = int(input())
if num > 0 :
    print("양수입니다.")
elif num < 0 : 
    print("음수입니다.")
else :
    print("0입니다")
```

```markdown
### 문제 6
사용자로부터 현재 월(`month`)을 입력받습니다. 다음 기준에 따라 봄인지 판별하세요.
- 3, 4, 5월: "봄"
```

```python
month = int(input())
if month in [3, 4, 5] :
    print("봄")

if month == 3 or month == 4 or month == 5:
    print('봄')
```

```markdown

### 문제 7
연도(`year`)를 입력받아 윤년인지 아닌지 판별하는 프로그램을 작성하세요.
- 조건 1: 연도가 4로 나누어 떨어지면 윤년입니다.
- 조건 2: 하지만 그중 100으로 나누어 떨어지는 연도는 윤년이 아닙니다.
- 조건 3: 단, 400으로 나누어 떨어지는 연도는 여전히 윤년입니다.

결과로 "윤년입니다" 또는 "평년입니다"를 출력하세요.
```

```python
year = int(input())

if (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0) :
    print("윤년입니다")
else :
    print("평년입니다")
```

```markdown
### 문제 8
영화관 입장료는 기본 12,000원입니다. 다음 조건에 따라 최종 요금을 계산하여 출력하세요.
- 나이가 13세 미만이면 50% 할인
- 나이가 65세 이상이면 70% 할인
- '쿠폰'이 있는 경우 추가로 2,000원 할인

최종적으로 "당신의 입장료는 OO원입니다."라고 출력하는 코드를 작성하세요.
```

```python
age = int(input("나이를 입력하세요 : "))
price = 12000

coupon = input("쿠폰 유무를 입력하세요 y/n : ")
if age >= 65 :
    price = price * 0.3
elif age < 13 :
    price = price * 0.5

if coupon == "y" :
    price = price - 2000

print(f"당신의 입장료는 {round(price)}입니다")
```

오랜만에 강의로 인해서 다시 보았는데 아직도 헷갈리는게 많다. 조금더 정진해야겠다…

우선 문제 파악하는 능력이 부족한 것 같아서 천천히 문제를 읽어봐야겠다.